---
phase: 01-price-discovery-i18n-shell
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - README.md
  - backend/README.md
  - frontend/README.md
files_deleted: []
autonomous: false
requirements: [PERF-01, I18N-05, A11Y-01]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A developer following README.md from a fresh clone can bring up the DB, run migrations, start the API, start the frontend, and see the dashboard with data"
    - "backend/README.md documents `alembic upgrade head`, the `docker compose down -v` / `alembic stamp head` reconciliation choice, the pytest command, INGEST_TRIGGER_SECRET, ARRIVALS_SOURCE_URL, and the no-live-arrivals limitation (PRICE-07)"
    - "frontend/README.md documents `node node_modules/next/dist/bin/next dev -p 3000`, the `npm run dev` backgrounding quirk, `npm run test`, and the NEXT_PUBLIC_API_URL default"
    - "With DB up + migrations applied + both servers running, GET /health is 200 and the dashboard at :3000 renders skeletons then data and switches en/hi/mr with no layout break"
    - "`cd backend && venv/Scripts/python.exe -m pytest -q` and `cd frontend && npm run test` are both green"
    - "No README pastes a real secret value (DATA_GOV_IN_API_KEY / INGEST_TRIGGER_SECRET) — placeholders only; backend/.env is not committed"
  artifacts:
    - path: "README.md"
      provides: "Root quickstart: prerequisites + the full green-run command sequence for both stacks"
      contains: "docker compose up -d db"
      min_lines: 40
    - path: "backend/README.md"
      provides: "Backend run + migrations + tests + env vars + the arrivals limitation"
      contains: "alembic upgrade head"
      min_lines: 40
    - path: "frontend/README.md"
      provides: "Frontend run + test + env, replacing the create-next-app boilerplate"
      contains: "node node_modules/next/dist/bin/next dev"
      min_lines: 20
  key_links:
    - from: "README.md"
      to: "backend/README.md"
      via: "root README links to backend/README.md for the migration/reconciliation detail"
      pattern: "backend/README\\.md"
    - from: "README.md"
      to: "frontend/README.md"
      via: "root README links to frontend/README.md for frontend specifics"
      pattern: "frontend/README\\.md"
---

<objective>
Deliver D-22's "Ship = green local run + PR" readiness: accurate start-command docs for both stacks and a verified end-to-end local run. The PR itself is created afterward by `/gsd-ship`.

Purpose: The repo currently has only a create-next-app boilerplate `frontend/README.md` and no root or backend README. A judge or teammate with no internet must be able to bring the whole app up from the docs alone, and the two new test suites plus the Alembic migration path must be documented as the canonical workflow.
Output: `README.md` (root, new), `backend/README.md` (new), `frontend/README.md` (rewritten), plus an automated integration check and a human visual sign-off of the running app.
</objective>

<execution_context>
@C:/Users/UMANG JAISWAL N/OneDrive/Desktop/SIH-21632/agrilink/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/UMANG JAISWAL N/OneDrive/Desktop/SIH-21632/agrilink/.claude/gsd-core/templates/summary.md
@C:/Users/UMANG JAISWAL N/OneDrive/Desktop/SIH-21632/agrilink/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-price-discovery-i18n-shell/1-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/CONCERNS.md

@docker-compose.yml
@backend/.env.example
@frontend/README.md
@.planning/phases/01-price-discovery-i18n-shell/01-01-SUMMARY.md
@.planning/phases/01-price-discovery-i18n-shell/01-02-SUMMARY.md
@.planning/phases/01-price-discovery-i18n-shell/01-03-SUMMARY.md
</context>

## Artifacts this phase produces

New files (across all 4 plans): `backend/alembic.ini`, `backend/alembic/env.py`, `backend/alembic/script.py.mako`, `backend/alembic/versions/0001_initial_schema.py`, `backend/app/services/snapshot.py`, `backend/app/services/data/maharashtra_snapshot.csv`, `backend/pyproject.toml`, `backend/tests/conftest.py`, `backend/tests/test_signal.py`, `backend/tests/test_geo.py`, `backend/tests/test_ingestion_normalize.py`, `backend/tests/test_ingestion_fallback.py`, `backend/tests/test_api_prices.py`, `backend/README.md`, `README.md`, `frontend/vitest.config.mts`, `frontend/vitest.setup.ts`, `frontend/src/test/render.tsx`, `frontend/src/components/AppShellSkeleton.tsx`, `frontend/src/i18n/types.d.ts`, `frontend/src/i18n/messages/parity.test.ts`, `frontend/src/components/SellWaitSignalCard.test.tsx`, `frontend/src/components/PriceDashboard.test.tsx`, `frontend/src/components/LanguageSwitcher.test.tsx`.

New symbols/config: `resolve_ingestion_rows()`, `fetch_arrivals_rows()` (off-by-default seam), `merge_arrivals()`, `load_snapshot_rows()`, `Settings.ingest_trigger_secret`, `Settings.arrivals_source_url`, `NAMING_CONVENTION` on `Base`, `max_distance_km` + `limit` on `GET /api/prices/nearby`, `x_ingest_secret` header on `POST /api/ingest/run`, `ready` gate + `AppShellSkeleton`, `renderWithIntl()`, `test` / `test:watch` scripts.

New env vars: `INGEST_TRIGGER_SECRET`, `ARRIVALS_SOURCE_URL` (both added to `backend/.env.example`).

This plan (01-04) contributes: `README.md` (root), `backend/README.md`, rewritten `frontend/README.md`, and the end-to-end green-run verification.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write root, backend, and frontend READMEs with the real start commands</name>
  <files>README.md, backend/README.md, frontend/README.md</files>
  <read_first>docker-compose.yml, backend/.env.example, frontend/README.md, .planning/codebase/STACK.md, .planning/codebase/CONCERNS.md, .planning/phases/01-price-discovery-i18n-shell/1-CONTEXT.md, .planning/phases/01-price-discovery-i18n-shell/01-01-SUMMARY.md, .planning/phases/01-price-discovery-i18n-shell/01-02-SUMMARY.md, .planning/phases/01-price-discovery-i18n-shell/01-03-SUMMARY.md</read_first>
  <action>
Create `README.md` at the repo root: a short "AgriLink - SIH 2026 PS 26132" intro (one paragraph, from PROJECT.md), a "Prerequisites" list (Docker + Docker Compose, Python 3.13 with `backend/venv`, Node + npm with `frontend/node_modules`), then a "Quickstart (local, offline-safe)" numbered sequence with these exact commands in order:
1. `docker compose up -d db`  (Postgres 16, host port 5433 - native PG18 holds 5432)
2. `cd backend && venv/Scripts/python.exe -m alembic upgrade head`  (first run only, or after pulling new migrations)
3. `cd backend && venv/Scripts/python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000`
4. `cd frontend && node node_modules/next/dist/bin/next dev -p 3000`  (NOT `npm run dev` - the wrapper exits code 1 when backgrounded in a non-TTY shell here)
5. open http://localhost:3000
Add a "Tests" section: `cd backend && venv/Scripts/python.exe -m pytest -q` and `cd frontend && npm run test`. Add a "Configuration" section pointing at `backend/.env` (copy from `backend/.env.example`; never commit it) listing `DATABASE_URL` (must point at :5433), `DATA_GOV_IN_API_KEY` (optional - blank triggers the snapshot/fixture fallback), `INGEST_TRIGGER_SECRET` (blank disables `POST /api/ingest/run`), `ARRIVALS_SOURCE_URL` (leave blank - no live arrivals source in Phase 1), and `frontend/.env` `NEXT_PUBLIC_API_URL` (defaults to `http://localhost:8000` when absent). Link to `backend/README.md` and `frontend/README.md` for detail. Use placeholder values only - never paste the real key from `backend/.env`.

Create `backend/README.md`: sections for (a) Run - the uvicorn command and the `docker compose up -d db` prerequisite; (b) Migrations - `venv/Scripts/python.exe -m alembic upgrade head` to apply, `venv/Scripts/python.exe -m alembic revision --autogenerate -m "..."` to add one, and the reconciliation note from the 01-01 checkpoint decision: primary path (record whichever the checkpoint selected - `docker compose down -v && docker compose up -d db && alembic upgrade head` for a clean baseline) and the alternative (`alembic stamp head` when preserving a DB whose schema already matches the models); state that the app also runs `alembic upgrade head` automatically at startup (idempotent); (c) Tests - `venv/Scripts/python.exe -m pytest -q`, note `-m "not pg"` skips the Postgres-only upsert test when no container is up; (d) Data sources - ingestion order is live data.gov.in -> committed `app/services/data/maharashtra_snapshot.csv` -> synthetic fixtures; the OGD price resource has NO arrivals field, so `arrival_volume` is null on live/snapshot data and the sell/wait signal's volume factor runs on fixture data only and says so in its reason - the real fix is tracked as PRICE-07 (v2); the `fetch_arrivals_rows()` seam is present but off unless `ARRIVALS_SOURCE_URL` is set; (e) Env vars - the same list as the root README's Configuration section.

Rewrite `frontend/README.md` (delete the create-next-app boilerplate entirely): sections for (a) Run - `node node_modules/next/dist/bin/next dev -p 3000` and why not `npm run dev` (CONCERNS.md: exits 1 when backgrounded here); `npm run build` for a production build; (b) Tests - `npm run test` (vitest run) and `npm run test:watch`; (c) i18n - locale is client-only (`localStorage['agrilink.locale']`, no `/[locale]` routing - Cordova constraint); `en.json` is the source of truth; `hi.json`/`mr.json` parity is enforced by `src/i18n/messages/parity.test.ts` in `npm run test`; (d) Config - `NEXT_PUBLIC_API_URL` (defaults to `http://localhost:8000`; set in `frontend/.env` to point elsewhere).
  </action>
  <verify>
    <automated>test -f README.md && test -f backend/README.md && grep -q "docker compose up -d db" README.md && grep -q "alembic upgrade head" backend/README.md && grep -q "node node_modules/next/dist/bin/next dev" frontend/README.md && grep -q "PRICE-07" backend/README.md && ! grep -q "$DATA_GOV_IN_API_KEY_LITERAL" README.md backend/README.md frontend/README.md && echo "DOCS_OK"</automated>
    <fails_when>Any required file is missing, OR a required command string is absent from its README, OR `PRICE-07` is not mentioned in `backend/README.md`, OR the real API key value appears in any README, OR `DOCS_OK` is not printed.</fails_when>
  </verify>
  <verify>
    <automated>grep -c "create-next-app" frontend/README.md</automated>
    <fails_when>The count is greater than 0 (the create-next-app boilerplate was not fully removed).</fails_when>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at repo root, contains `docker compose up -d db`, the `alembic upgrade head` step, the uvicorn command with `--port 8000`, `node node_modules/next/dist/bin/next dev -p 3000`, and a Tests section naming both `pytest -q` and `npm run test`
    - `backend/README.md` contains `alembic upgrade head`, both reconciliation options (`down -v` and `alembic stamp head`), `pytest`, `INGEST_TRIGGER_SECRET`, `ARRIVALS_SOURCE_URL`, and `PRICE-07`
    - `frontend/README.md` contains `node node_modules/next/dist/bin/next dev`, `npm run test`, `NEXT_PUBLIC_API_URL`, and no `create-next-app` text
    - No README contains the real `DATA_GOV_IN_API_KEY` value or any real `INGEST_TRIGGER_SECRET` value
    - `README.md` links to both `backend/README.md` and `frontend/README.md`
  </acceptance_criteria>
  <done>All three READMEs describe the actual, working local run for this repo (ports, migration path, tests, env) with placeholders for secrets.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Automated end-to-end integration check</name>
  <files>README.md</files>
  <read_first>README.md, backend/README.md, docker-compose.yml, backend/.env.example</read_first>
  <precondition>Docker is available; `backend/venv` has the deps from 01-01/01-02 (alembic, pytest); `frontend/node_modules` has the deps from 01-03 (vitest set). Plans 01-02 and 01-03 are merged.</precondition>
  <action>
Run the documented green-run sequence exactly as README.md describes it and confirm each step, capturing output for the SUMMARY. This task changes no code — if a step fails, the fix belongs in the owning plan (01-01/01-02/01-03) and this plan reports the gap rather than patching around it. If README.md wording proved inaccurate during the run, correct README.md (and backend/frontend READMEs) to match what actually worked.

Steps:
1. `docker compose up -d db` and wait for `pg_isready` (healthcheck) to pass.
2. From a clean schema (apply the reconciliation path the 01-01 checkpoint selected), `cd backend && venv/Scripts/python.exe -m alembic upgrade head` - expect exit 0 and `alembic_version` + 8 model tables present.
3. `cd backend && venv/Scripts/python.exe -m pytest -q` - expect all non-`pg` tests green.
4. `cd frontend && npm run test` - expect all vitest suites green.
5. Start `uvicorn app.main:app --host 127.0.0.1 --port 8000` in the background; `curl` `GET /health` (expect 200) and `GET /api/prices/trend?crop=Onion&market=Pune&days=30` (expect a body with `"points"` and at least one entry); `curl -X POST /api/ingest/run` with no header (expect 403).
6. Start `node node_modules/next/dist/bin/next dev -p 3000` in the background; `curl -I http://localhost:3000` (expect 200) and confirm the HTML contains the app shell.
7. Stop both background servers.
  </action>
  <verify>
    <automated>docker compose up -d db && sleep 6 && cd backend && venv/Scripts/python.exe -m alembic upgrade head && venv/Scripts/python.exe -m pytest -q -m "not pg" && cd ../frontend && npm run test && cd .. && (cd backend && venv/Scripts/python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000 &) && sleep 8 && curl -sS -o /dev/null -w "health=%{http_code}\n" http://127.0.0.1:8000/health && curl -sS "http://127.0.0.1:8000/api/prices/trend?crop=Onion&market=Pune&days=30" | grep -q '"points"' && curl -sS -o /dev/null -w "ingest_noauth=%{http_code}\n" -X POST http://127.0.0.1:8000/api/ingest/run && pkill -f "uvicorn app.main:app --host 127.0.0.1 --port 8000"</automated>
    <fails_when>`alembic upgrade head` exits non-zero, OR `pytest` reports FAILED/ERROR, OR `npm run test` reports FAIL, OR `health=200` is not printed, OR the trend response lacks `"points"`, OR `ingest_noauth=403` is not printed.</fails_when>
  </verify>
  <verify>
    <automated>cd frontend && (node node_modules/next/dist/bin/next dev -p 3000 &) && sleep 12 && curl -sS -o /dev/null -w "front=%{http_code}\n" http://localhost:3000 && pkill -f "next/dist/bin/next dev -p 3000"</automated>
    <fails_when>`front=200` is not printed (the Next dev server did not serve the app), OR the process failed to start within the wait window.</fails_when>
  </verify>
  <acceptance_criteria>
    - `docker compose up -d db` -> healthcheck passes
    - `alembic upgrade head` exits 0 on a freshly reconciled schema; 8 model tables + `alembic_version` present
    - `venv/Scripts/python.exe -m pytest -q -m "not pg"` is fully green
    - `npm run test` (vitest) is fully green
    - `GET /health` -> 200; `GET /api/prices/trend?...` -> body with `"points"`; `POST /api/ingest/run` with no header -> 403
    - `curl http://localhost:3000` -> 200
  </acceptance_criteria>
  <done>The exact command sequence in README.md brings the whole app up from a clean database with both test suites green and the three key endpoints behaving.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: human visual sign-off of the running Phase 1 app</name>
  <what-built>The full Phase 1 app is running locally: FastAPI on http://127.0.0.1:8000 (migrated via Alembic, no create_all), Next.js dev on http://localhost:3000. Backend pytest and frontend vitest suites are green. READMEs at repo root, backend/, and frontend/ describe this exact run.</what-built>
  <how-to-verify>Open http://localhost:3000 and check, visually only:
  1. On load you briefly see grey skeleton blocks, then the price dashboard fills in with a trend chart, a sell/wait card whose reasons show real rupee numbers, and a nearby-markets table (nearest first).
  2. The header language switcher offers English / हिंदी / मराठी. Switch to हिंदी: all visible copy changes to Devanagari, the layout does not break or reflow awkwardly, and there is no flash of English on a manual page refresh. Switch to मराठी: same. Switch back to English.
  3. Tap targets (selects, buttons, switcher) are comfortably large; focus outlines are visible when tabbing; colours are earthy green/ochre, not startup-blue.
  4. If reachable, momentarily stop the backend and reload: the dashboard shows an error message WITH a "Retry" button (not just static text). Restart the backend and click Retry: data returns.</how-to-verify>
  <resume-signal>Type "approved" if all four checks pass, or describe what looked wrong (which plan it maps to).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs -> reader | READMEs may leak secrets or commit-sensitive files if written carelessly |
| local dev servers -> host network | The green-run binds HTTP ports on the dev machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | READMEs pasting the real `DATA_GOV_IN_API_KEY` / an `INGEST_TRIGGER_SECRET` value | medium | mitigate | READMEs use placeholders and point at `backend/.env.example`; a verify step greps all three READMEs for the known real key string and fails if present |
| T-04-02 | Information Disclosure | `backend/.env` getting committed alongside the doc changes | medium | mitigate | `.gitignore` already excludes `.env` (verified in repo); README states "never commit `backend/.env`"; the phase commit only stages the three README paths |
| T-04-03 | Denial of Service | Green-run dev servers left running / bound to all interfaces | low | accept | uvicorn bound to `127.0.0.1`; Next dev on localhost; Task 2 stops both background servers; demo-only machine |
| T-04-SC | Tampering | package installs | n/a | accept | This plan installs nothing; all dependency additions and their legitimacy checkpoints happened in 01-01/01-02/01-03 |

Block-on-high check: no `high`-severity items in this plan. No blocking security issue.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `README.md`, `backend/README.md`, `frontend/README.md` exist with the exact commands listed in Task 1's acceptance criteria
- [ ] No README contains the real API key value; `frontend/README.md` has no `create-next-app` boilerplate
- [ ] Automated green-run: `alembic upgrade head` clean, `pytest -q -m "not pg"` green, `npm run test` green, `/health` 200, `/api/prices/trend` has `points`, `POST /api/ingest/run` no-header -> 403, `curl :3000` -> 200
- [ ] Human visual sign-off: skeletons->data, en/hi/mr switch with no layout break and no English flash, large tap targets + visible focus + earthy palette, error state shows a working Retry
</verification>

<success_criteria>
- All tasks completed; the human-verify checkpoint is approved
- The documented local run is reproducible from a clean DB (D-22 "green local run")
- Both test suites green; three READMEs accurate
- Phase 1 is ready for `/gsd-ship` to open the PR to `github.com/umangjzx/Sih-26132`
</success_criteria>

<output>
After completion, create `.planning/phases/01-price-discovery-i18n-shell/01-04-SUMMARY.md`
</output>
