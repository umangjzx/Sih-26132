---
phase: 01-price-discovery-i18n-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/database.py
  - backend/app/main.py
  - backend/requirements.txt
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
  - backend/alembic/versions/0001_initial_schema.py
files_deleted: []
autonomous: false
requirements: [PRICE-01]
user_setup:
  - service: docker-postgres
    why: "Alembic migrations and the startup upgrade run against the Docker Postgres on host port 5433"
    dashboard_config:
      - task: "Start the database container before executing this plan"
        location: "repo root shell"
        details: "docker compose up -d db  (maps host 5433 -> container 5432; native PG18 holds 5432)"

estimate:
  tokens: 70000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Running `alembic upgrade head` from backend/ against an empty database creates alembic_version plus all 8 model tables (price_cache, users, lots, demands, matches, offers, deals, disputes)"
    - "The FastAPI app boots without calling Base.metadata.create_all — schema is created only by the Alembic upgrade in the lifespan"
    - "GET /health returns 200 and GET /api/prices/trend returns at least one price point after a fresh start (fixture fallback seeds data)"
    - "Re-running `alembic upgrade head` on an already-migrated database is a no-op that exits 0"
    - "`alembic revision --autogenerate` on an up-to-date database produces a migration with no create_table / drop_table operations (env.py sees every model)"
  artifacts:
    - path: "backend/alembic/env.py"
      provides: "Alembic online migration entrypoint wired to Base.metadata and settings.database_url"
      contains: "target_metadata = Base.metadata"
      min_lines: 25
    - path: "backend/alembic/versions/0001_initial_schema.py"
      provides: "Initial autogenerated migration covering price_cache + the 7 dormant Pillar B/C tables"
      contains: "op.create_table"
      min_lines: 60
    - path: "backend/alembic.ini"
      provides: "Alembic config; script_location points at the alembic/ package"
      contains: "script_location"
    - path: "backend/app/core/database.py"
      provides: "Base with a deterministic MetaData naming convention"
      contains: "naming_convention"
    - path: "backend/app/main.py"
      provides: "Lifespan that runs alembic upgrade head instead of create_all"
      contains: "command.upgrade"
    - path: "backend/requirements.txt"
      provides: "Pinned alembic dependency"
      contains: "alembic=="
  key_links:
    - from: "backend/alembic/env.py"
      to: "backend/app/core/database.py"
      via: "imports Base and sets target_metadata = Base.metadata; imports app.models so all 8 tables register"
      pattern: "from app.core.database import Base"
    - from: "backend/alembic/env.py"
      to: "backend/app/core/config.py"
      via: "config.set_main_option('sqlalchemy.url', settings.database_url)"
      pattern: "settings.database_url"
    - from: "backend/app/main.py"
      to: "backend/alembic.ini"
      via: "lifespan builds AlembicConfig(str(ALEMBIC_INI)) and calls command.upgrade(cfg, 'head')"
      pattern: "command.upgrade"
---

<objective>
Adopt Alembic as the single schema authority for the backend and retire `Base.metadata.create_all` (D-11, D-12). This is the phase tracer: prove the whole persistence path end to end — model metadata -> env.py -> autogenerated migration -> Postgres -> app boot -> a live `/api/prices/*` response — before any other Phase 1 work builds on it.

Purpose: Phase 2 auth work needs a real migration baseline that already contains the dormant Pillar B/C tables. Two schema authorities (create_all + Alembic) hide migration bugs, so create_all must go.
Output: `backend/alembic/` package, `backend/alembic.ini`, one reviewed `0001_initial_schema.py` migration, a `MetaData(naming_convention=...)` on `Base`, a lifespan that runs `alembic upgrade head` idempotently, and `alembic==1.19.1` pinned in `requirements.txt`.
</objective>

<execution_context>
@C:/Users/UMANG JAISWAL N/OneDrive/Desktop/SIH-21632/agrilink/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/UMANG JAISWAL N/OneDrive/Desktop/SIH-21632/agrilink/.claude/gsd-core/templates/summary.md
@C:/Users/UMANG JAISWAL N/OneDrive/Desktop/SIH-21632/agrilink/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-price-discovery-i18n-shell/1-CONTEXT.md
@.planning/phases/01-price-discovery-i18n-shell/1-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md

@backend/app/core/database.py
@backend/app/core/config.py
@backend/app/main.py
@backend/app/models/__init__.py
@backend/app/models/price_cache.py
@backend/requirements.txt
@docker-compose.yml
</context>

## Artifacts this phase produces

New files (across all 4 plans): `backend/alembic.ini`, `backend/alembic/env.py`, `backend/alembic/script.py.mako`, `backend/alembic/versions/0001_initial_schema.py`, `backend/app/services/snapshot.py`, `backend/app/services/data/maharashtra_snapshot.csv`, `backend/pyproject.toml`, `backend/tests/conftest.py`, `backend/tests/test_signal.py`, `backend/tests/test_geo.py`, `backend/tests/test_ingestion_normalize.py`, `backend/tests/test_ingestion_fallback.py`, `backend/tests/test_api_prices.py`, `backend/README.md`, `README.md`, `frontend/vitest.config.mts`, `frontend/vitest.setup.ts`, `frontend/src/test/render.tsx`, `frontend/src/components/AppShellSkeleton.tsx`, `frontend/src/i18n/types.d.ts`, `frontend/src/i18n/messages/parity.test.ts`, `frontend/src/components/SellWaitSignalCard.test.tsx`, `frontend/src/components/PriceDashboard.test.tsx`, `frontend/src/components/LanguageSwitcher.test.tsx`.

New symbols/config: `resolve_ingestion_rows()`, `fetch_arrivals_rows()` (off-by-default seam), `merge_arrivals()`, `load_snapshot_rows()`, `Settings.ingest_trigger_secret`, `Settings.arrivals_source_url`, `NAMING_CONVENTION` + `MetaData(naming_convention=...)` on `Base`, `max_distance_km` + `limit` query params on `GET /api/prices/nearby`, `x_ingest_secret` header on `POST /api/ingest/run`, `ready` gate + `AppShellSkeleton` in `LocaleProvider`, `renderWithIntl()`, `test` / `test:watch` scripts.

New env vars: `INGEST_TRIGGER_SECRET`, `ARRIVALS_SOURCE_URL` (both added to `backend/.env.example`).

This plan (01-01) contributes: `backend/alembic.ini`, `backend/alembic/env.py`, `backend/alembic/script.py.mako`, `backend/alembic/versions/0001_initial_schema.py`, `NAMING_CONVENTION` + naming convention on `Base` in `database.py`, `command.upgrade` lifespan in `main.py`, `alembic==1.19.1` in `requirements.txt`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: verify alembic package legitimacy before install</name>
  <what-built>About to add one new backend dependency: `alembic==1.19.1` (official SQLAlchemy sub-project, the de-facto standard migration tool). The Package Legitimacy Audit in 1-RESEARCH.md flagged it `SUS` — this is a recency false positive (`too-new` fires on frequent minor releases; PyPI exposes no weekly download count to the audit seam). No `postinstall`/build hooks.</what-built>
  <how-to-verify>Open https://pypi.org/project/alembic/ and confirm: project is `alembic`, maintained under `github.com/sqlalchemy/alembic`, latest release is 1.19.x dated 2026-08, downloads are in the millions/week. This is expected to be a trivial ACK.</how-to-verify>
  <resume-signal>Type "approved" to proceed with `pip install alembic==1.19.1`, or describe concerns.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: choose the DB reconciliation path before the Alembic cutover</name>
  <decision>How to reconcile any pre-existing create_all-built database with the new Alembic baseline on machines where the Docker volume `agrilink_pgdata` already has tables but no `alembic_version` row. This must be settled before Task 1's verify runs `DROP SCHEMA public CASCADE`.</decision>
  <context>Task 1 carries a `one-way` reversibility rating: the clean path drops the volume. 1-RESEARCH.md "Runtime State Inventory" confirms `price_cache` is regenerable cache and nothing else is persisted, but the choice is still a one-way door for option `down-v`. The selected path is documented in `backend/README.md` (written in plan 01-04).</context>
  <options>
    <option id="down-v">
      <name>docker compose down -v, then up, then alembic upgrade head</name>
      <pros>Guaranteed-clean baseline; matches the Task 1 verify; simplest to document; the only state lost is regenerable price cache.</pros>
      <cons>Destroys the volume — irreversible for any hand-entered rows (there are none in Phase 1).</cons>
    </option>
    <option id="stamp">
      <name>alembic stamp head against the existing schema</name>
      <pros>Preserves all existing data; no container recreation.</pros>
      <cons>Only correct if the live schema already matches the models exactly; silently diverges if not; harder to reason about for a demo.</cons>
    </option>
  </options>
  <resume-signal>Select: down-v or stamp (the selected one becomes the primary instruction in backend/README.md; the other is documented as the alternative).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Alembic scaffold + initial migration + lifespan cutover (TRACER — end-to-end persistence path)</name>
  <files>backend/app/core/database.py, backend/alembic.ini, backend/alembic/env.py, backend/alembic/script.py.mako, backend/alembic/versions/0001_initial_schema.py, backend/app/main.py, backend/requirements.txt</files>
  <read_first>backend/app/core/database.py, backend/app/core/config.py, backend/app/main.py, backend/app/models/__init__.py, backend/app/models/price_cache.py, backend/requirements.txt, .planning/phases/01-price-discovery-i18n-shell/1-RESEARCH.md</read_first>
  <precondition>Docker Postgres is running and reachable at the `DATABASE_URL` in `backend/.env` (`postgresql+psycopg2://agrilink:agrilink@localhost:5433/agrilink`) — run `docker compose up -d db` from the repo root first.</precondition>
  <reversibility rating="one-way">Replacing `create_all` with an Alembic baseline against the existing Docker volume is a one-way schema cutover: the existing volume has create_all-built tables and no `alembic_version` row, so the clean path drops the volume (see the checkpoint:decision that follows). Price data in `price_cache` is regenerable cache, nothing else is persisted.</reversibility>
  <action>
Install alembic into the venv: `backend/venv/Scripts/python.exe -m pip install alembic==1.19.1`, then append `alembic==1.19.1` to `backend/requirements.txt` (keep the file's existing ordering; add it after `apscheduler==3.11.0`).

Add a deterministic constraint naming convention to `Base` in `backend/app/core/database.py` per 1-RESEARCH.md "Pattern 2": define a module-level `NAMING_CONVENTION` dict with keys `ix`, `uq`, `ck`, `fk`, `pk` using the exact templates from that pattern, and give `Base` a class-level `metadata = MetaData(naming_convention=NAMING_CONVENTION)` (import `MetaData` from `sqlalchemy`). Do not rename `uq_price_cache_key` — `price_cache` already names its unique constraint explicitly and that name is preserved. The `ingestion.upsert_price_rows` call targets `index_elements=["market","crop","variety","date"]` (a column list, not a constraint name) so it is unaffected.

Run `cd backend && venv/Scripts/python.exe -m alembic init alembic` to generate `backend/alembic.ini`, `backend/alembic/env.py`, `backend/alembic/script.py.mako`, and the empty `backend/alembic/versions/` directory.

Rewrite `backend/alembic/env.py` per 1-RESEARCH.md "Pattern 1": import `settings` from `app.core.config`, import `Base` from `app.core.database`, and `import app.models  # noqa: F401` so all 8 tables register on `Base.metadata` (Pitfall 1 — without this the migration is empty). Set `config.set_main_option("sqlalchemy.url", settings.database_url)`. Set `target_metadata = Base.metadata`. In the online path pass `compare_type=True` and `compare_server_default=True` to `context.configure`. Keep only the online path (offline mode is not needed for this project). In `backend/alembic.ini` leave `script_location = alembic` and leave the placeholder `sqlalchemy.url` line as-is (env.py overrides it at runtime).

Generate the initial migration: `cd backend && venv/Scripts/python.exe -m alembic revision --autogenerate -m "initial schema"`, then rename the produced file to `backend/alembic/versions/0001_initial_schema.py` and set its `revision = "0001"` and `down_revision = None`. Open the file and confirm `upgrade()` contains `op.create_table("price_cache", ...)` plus `op.create_table` for `users`, `lots`, `demands`, `matches`, `offers`, `deals`, `disputes` (8 tables total). If `upgrade()` is empty or missing tables, fix the `import app.models` line in env.py and regenerate.

Cut over the lifespan in `backend/app/main.py` per 1-RESEARCH.md "Pattern 3": add `ALEMBIC_INI = Path(__file__).resolve().parents[1] / "alembic.ini"` and, as the first action inside the `lifespan` async context manager, call `command.upgrade(AlembicConfig(str(ALEMBIC_INI)), "head")` wrapped in a `try/except` that logs `"alembic upgrade failed - run 'cd backend && alembic upgrade head' manually"` via `logger.exception(...)` and re-raises. Import `command` from `alembic` and `Config as AlembicConfig` from `alembic.config`. Remove the schema-bootstrap call that currently runs at the top of the lifespan (the `Base.metadata.create_all(bind=engine)` invocation) — the Alembic upgrade is now the only schema mechanism (Anti-Pattern: keeping both). Keep the existing "if not has_price_data -> run_ingestion" block and the scheduler start exactly as they are, after the upgrade call. The `engine` import in `main.py` may become unused — drop it from the import only if nothing else references it.
  <!-- planner-discipline-allow: create_all -->
  <!-- planner-discipline-allow: Base.metadata.create_all -->
  </action>
  <verify>
    <automated>docker compose up -d db && docker compose exec -T db psql -U agrilink -d agrilink -c "DROP SCHEMA public CASCADE; CREATE SCHEMA public;" && cd backend && venv/Scripts/python.exe -m alembic upgrade head && venv/Scripts/python.exe -c "import sqlalchemy as sa; from app.core.config import settings; e=sa.create_engine(settings.database_url); insp=sa.inspect(e); t=set(insp.get_table_names()); need={'alembic_version','price_cache','users','lots','demands','matches','offers','deals','disputes'}; missing=need-t; print('MISSING',missing); assert not missing"</automated>
    <fails_when>`alembic upgrade head` exits non-zero, OR the python assert raises (prints `MISSING {...}` with a non-empty set), OR `alembic_version` / any of the 8 model tables is absent from the inspected schema.</fails_when>
  </verify>
  <verify>
    <automated>cd backend && (venv/Scripts/python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8011 &) ; sleep 8 ; curl -sS -o /dev/null -w "%{http_code}" http://127.0.0.1:8011/health ; echo ; curl -sS "http://127.0.0.1:8011/api/prices/trend?crop=Onion&market=Pune&days=30" | head -c 200 ; pkill -f "uvicorn app.main:app --host 127.0.0.1 --port 8011"</automated>
    <fails_when>`/health` does not return `200`, OR the `/api/prices/trend` response does not contain `"points"` with at least one object, OR uvicorn logs an exception during startup (alembic upgrade failure, ImportError).</fails_when>
  </verify>
  <acceptance_criteria>
    - `backend/app/core/database.py` contains `naming_convention` and `MetaData(naming_convention=NAMING_CONVENTION)`
    - `backend/alembic/env.py` contains `target_metadata = Base.metadata`, `import app.models`, and `settings.database_url`
    - `backend/alembic/versions/0001_initial_schema.py` contains `op.create_table("price_cache"` and 8 total `op.create_table(` calls; `revision = "0001"`; `down_revision = None`
    - `backend/app/main.py` contains `command.upgrade` and `ALEMBIC_INI`; `grep -c "create_all" backend/app/main.py` returns `0`
    - `backend/requirements.txt` contains a line matching `^alembic==1\.19\.1$`
    - `cd backend && venv/Scripts/python.exe -m alembic upgrade head` exits 0 against a freshly emptied `public` schema and the inspector then finds all 9 required table names
    - Booting `uvicorn app.main:app` yields `GET /health` -> `200` and `GET /api/prices/trend?crop=Onion&market=Pune&days=30` -> body containing `"points"`
  </acceptance_criteria>
  <done>On a clean database `alembic upgrade head` builds the full 8-table schema plus `alembic_version`, the app boots with no `create_all` call, and a real `/api/prices/*` route still returns data.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Idempotency, empty-autogen guard, and CORS narrowing</name>
  <files>backend/alembic/env.py, backend/app/main.py</files>
  <read_first>backend/alembic/env.py, backend/app/main.py, backend/app/core/config.py, .planning/phases/01-price-discovery-i18n-shell/1-RESEARCH.md</read_first>
  <action>
Confirm the startup upgrade is idempotent: with the schema already at head, `command.upgrade(cfg, "head")` must be a no-op that returns without error. If Task 1's try/except is correct this already holds — add a short comment above the call in `backend/app/main.py` noting it is idempotent and that the manual fallback command is `cd backend && alembic upgrade head` (documented in `backend/README.md`).

Narrow CORS in `backend/app/main.py` per 1-RESEARCH.md Security Domain V14 and CONCERNS.md: change `allow_methods=["*"]` to `allow_methods=["GET", "POST"]` and remove `allow_credentials=True` (there is no auth, no cookies, no JWT until Phase 2). Keep `allow_origins=settings.cors_origin_list` and `allow_headers=["*"]` unchanged. Add a one-line comment that credentials/methods widen again in Phase 2 when auth lands.

Verify the env.py wiring is complete enough that a no-change autogenerate is empty (Pitfall 1 regression guard): after `alembic upgrade head`, running `alembic revision --autogenerate -m "noop check"` must produce an `upgrade()` body with no `op.create_table` / `op.drop_table` / `op.add_column` calls. Delete that throwaway check migration file afterward — do not commit it.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m alembic upgrade head && venv/Scripts/python.exe -m alembic upgrade head && echo "IDEMPOTENT_OK"</automated>
    <fails_when>Either `alembic upgrade head` invocation exits non-zero, OR `IDEMPOTENT_OK` is not printed (a second upgrade against head must not error).</fails_when>
  </verify>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m alembic revision --autogenerate -m "noop check" -x throwaway=1 && ls alembic/versions/*noop_check* && grep -E "op\.(create_table|drop_table|add_column|drop_column)" alembic/versions/*noop_check* ; echo "grep_exit=$?" ; rm -f alembic/versions/*noop_check*</automated>
    <fails_when>`grep_exit=0` (meaning schema-changing operations were found in the no-op autogenerate — env.py is not seeing all models), OR the revision command exits non-zero.</fails_when>
  </verify>
  <verify>
    <automated>grep -E 'allow_methods=\["GET", "POST"\]' backend/app/main.py && ! grep -q 'allow_credentials=True' backend/app/main.py && echo "CORS_NARROWED_OK"</automated>
    <fails_when>`allow_methods=["GET", "POST"]` is absent, OR `allow_credentials=True` is still present, OR `CORS_NARROWED_OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Two consecutive `alembic upgrade head` runs both exit 0 and the second prints `IDEMPOTENT_OK`
    - A fresh `--autogenerate` against an up-to-date DB yields a migration whose `upgrade()` contains no `op.create_table` / `op.drop_table` / `op.add_column` / `op.drop_column` (grep exits non-zero); the throwaway file is removed and not committed
    - `backend/app/main.py` contains `allow_methods=["GET", "POST"]` and does NOT contain `allow_credentials=True`
    - `backend/app/main.py` has a comment near `command.upgrade` naming the manual fallback `cd backend && alembic upgrade head`
  </acceptance_criteria>
  <done>Startup upgrade is proven idempotent, env.py is proven to see all 8 models (empty no-op autogenerate), and CORS is narrowed to GET/POST with no credentials for the no-auth phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell / CI -> Alembic -> Postgres | Migration scripts execute arbitrary DDL against the demo database |
| FastAPI process startup -> Alembic upgrade | App boot runs `command.upgrade` before serving traffic |
| pip -> backend/venv | New third-party package (`alembic`) enters the runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `backend/alembic/env.py` sqlalchemy.url | medium | mitigate | `env.py` sets the URL from `settings.database_url` (single source, pydantic-settings from `backend/.env`); Task 1 verify inspects the :5433 DB for `alembic_version` to prove it hit the right database; README instructs running `alembic` from `backend/` (Pitfall 2) |
| T-01-02 | Denial of Service | `backend/app/main.py` lifespan `command.upgrade` | low | mitigate | Upgrade wrapped in try/except with `logger.exception` and re-raise; manual `alembic upgrade head` documented as the non-startup path; single-worker uvicorn for the demo means no migration race |
| T-01-03 | Information Disclosure | Alembic / SQLAlchemy engine logging of the DB URL with credentials | low | accept | Credentials are the local demo pair `agrilink/agrilink` on `localhost:5433`, not a secret; app code does not log `settings.database_url`; `fileConfig` uses Alembic's default logger levels |
| T-01-SC | Tampering | `pip install alembic==1.19.1` | high | mitigate | Package-legitimacy `checkpoint:human-verify` before install (verify on pypi.org/project/alembic — official SQLAlchemy sub-project); version pinned exactly in `requirements.txt`; audit confirms no `postinstall` hook; `SUS` verdict is a documented recency false-positive |

Block-on-high check: T-01-SC is the only `high` item and its disposition is `mitigate` via a blocking human checkpoint plus an exact version pin — no unmitigated `high` remains.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd backend && venv/Scripts/python.exe -m alembic upgrade head` exits 0 on a clean `public` schema and creates all 8 model tables + `alembic_version`
- [ ] A second `alembic upgrade head` is a clean no-op (`IDEMPOTENT_OK`)
- [ ] `alembic revision --autogenerate` on an up-to-date DB produces no schema operations (throwaway file deleted)
- [ ] `grep -c "create_all" backend/app/main.py` returns `0`; `grep -c "command.upgrade" backend/app/main.py` returns `>=1`
- [ ] `uvicorn app.main:app` boots; `GET /health` -> 200; `GET /api/prices/trend?crop=Onion&market=Pune&days=30` -> body with `"points"`
- [ ] `backend/requirements.txt` pins `alembic==1.19.1`
- [ ] `backend/app/main.py` CORS is `allow_methods=["GET", "POST"]` with no `allow_credentials=True`
</verification>

<success_criteria>
- All tasks completed; both checkpoints resolved
- Alembic is the single schema authority; `create_all` is gone from `main.py`
- Initial migration covers `price_cache` + all 7 dormant Pillar B/C tables (Phase 2 baseline ready)
- Startup upgrade is idempotent and safe; manual command documented for 01-04's README
- No new errors or warnings during app startup
</success_criteria>

<output>
After completion, create `.planning/phases/01-price-discovery-i18n-shell/01-01-SUMMARY.md`
</output>
