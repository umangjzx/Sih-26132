---
phase: 01-price-discovery-i18n-shell
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/app/services/ingestion.py
  - backend/app/services/snapshot.py
  - backend/app/services/data/maharashtra_snapshot.csv
  - backend/app/api/prices.py
  - backend/app/core/config.py
  - backend/.env.example
  - backend/requirements.txt
  - backend/pyproject.toml
  - backend/tests/conftest.py
  - backend/tests/test_signal.py
  - backend/tests/test_geo.py
  - backend/tests/test_ingestion_normalize.py
  - backend/tests/test_ingestion_fallback.py
  - backend/tests/test_api_prices.py
files_deleted: []
autonomous: false
requirements: [PRICE-01, PRICE-02, PRICE-03, PRICE-04, PRICE-05, PRICE-06]
user_setup:
  - service: ingest-trigger-secret
    why: "POST /api/ingest/run returns 403 until a shared secret is configured (D-05); the developer picks the value"
    env_vars:
      - name: INGEST_TRIGGER_SECRET
        source: "Choose any long random string; add it to backend/.env (gitignored). Leave blank to keep the endpoint disabled."

estimate:
  tokens: 95000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "With no DATA_GOV_IN_API_KEY set, run_ingestion returns source 'snapshot' (committed CSV) or 'fixture' and PriceCache is populated so the dashboard always has data"
    - "resolve_ingestion_rows() decides the source and returns rows with no database access or side effects, and is unit-tested without a DB"
    - "GET /api/prices/nearby accepts max_distance_km (default 200) and limit (default 8), drops known-distance markets beyond the cap, keeps unknown-centroid markets sorting last, and returns at most `limit` rows"
    - "POST /api/ingest/run returns 403 when the X-Ingest-Secret header is missing or wrong, and 200 when it matches INGEST_TRIGGER_SECRET via a constant-time compare"
    - "compute_signal still emits the 'Arrival-volume data isn't available for this market, so this factor was skipped.' reason for rows with null arrival_volume (D-07 dormant volume factor)"
    - "fetch_arrivals_rows() is present but off by default (no live source wired) and merge_arrivals() joins volume onto existing PriceCache rows on (market, crop, date) without creating price-less rows"
    - "`cd backend && venv/Scripts/python.exe -m pytest -q` passes for all backend test files"
  artifacts:
    - path: "backend/app/services/ingestion.py"
      provides: "resolve_ingestion_rows(), fetch_arrivals_rows() off-by-default seam, merge_arrivals()"
      contains: "def resolve_ingestion_rows"
      min_lines: 120
    - path: "backend/app/services/snapshot.py"
      provides: "load_snapshot_rows() — stdlib csv reader over the committed Maharashtra export"
      contains: "def load_snapshot_rows"
      min_lines: 20
    - path: "backend/app/services/data/maharashtra_snapshot.csv"
      provides: "Committed real Maharashtra data.gov.in export (10-field schema, authentic names/prices, no arrivals)"
      contains: "modal_price"
      min_lines: 30
    - path: "backend/app/api/prices.py"
      provides: "nearby cap+limit; ingest shared-secret gate"
      contains: "max_distance_km"
      min_lines: 120
    - path: "backend/app/core/config.py"
      provides: "ingest_trigger_secret and arrivals_source_url settings"
      contains: "ingest_trigger_secret"
    - path: "backend/tests/conftest.py"
      provides: "SQLite StaticPool engine fixture, seeded_db, get_db override, bare TestClient"
      contains: "StaticPool"
      min_lines: 30
    - path: "backend/tests/test_signal.py"
      provides: "D-08 cases: sell_now / wait / hold / <7 None / 7-13 no-MA30 / volume present / volume absent"
      contains: "def test_"
      min_lines: 40
    - path: "backend/tests/test_api_prices.py"
      provides: "One smoke test per /api/prices/* + /api/options + the D-05 403/200 pair"
      contains: "def test_ingest_requires_secret"
      min_lines: 40
    - path: "backend/pyproject.toml"
      provides: "pytest config: pythonpath, testpaths, pg marker"
      contains: "[tool.pytest.ini_options]"
    - path: "backend/requirements.txt"
      provides: "Pinned pytest dependency"
      contains: "pytest=="
  key_links:
    - from: "backend/app/services/ingestion.py"
      to: "backend/app/services/snapshot.py"
      via: "resolve_ingestion_rows() calls load_snapshot_rows() as the second source before generate_fixture_rows()"
      pattern: "load_snapshot_rows"
    - from: "backend/app/api/prices.py"
      to: "backend/app/core/config.py"
      via: "trigger_ingestion reads settings.ingest_trigger_secret and compares with secrets.compare_digest"
      pattern: "secrets.compare_digest"
    - from: "backend/tests/test_api_prices.py"
      to: "backend/tests/conftest.py"
      via: "client fixture overrides get_db with a SQLite seeded_db and uses bare TestClient(app)"
      pattern: "TestClient\\(app\\)"
    - from: "backend/tests/test_ingestion_fallback.py"
      to: "backend/app/services/ingestion.py"
      via: "monkeypatches fetch_maharashtra_rows to raise / return empty and asserts resolve_ingestion_rows falls through"
      pattern: "resolve_ingestion_rows"
---

<objective>
Complete Pillar A's data layer and API surface to demo quality and give the backend its first test suite (D-02 revised, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-13).

Purpose: The signal's volume factor must run on real committed data (not only the synthetic generator), the write endpoint must not be anonymously triggerable before Phase 2 auth, `/nearby` must be bounded, and the pure logic (`compute_signal`, `district_distance_km`, `normalize_rows`, the new `resolve_ingestion_rows`) must be locked down by tests.
Output: `resolve_ingestion_rows()` refactor, a committed Maharashtra CSV snapshot + `load_snapshot_rows()`, an off-by-default `fetch_arrivals_rows()` seam + `merge_arrivals()`, `/nearby` `max_distance_km`+`limit`, the `POST /api/ingest/run` shared-secret gate, and `backend/tests/` (pytest infra + 5 test modules).
</objective>

<execution_context>
@C:/Users/UMANG JAISWAL N/OneDrive/Desktop/SIH-21632/agrilink/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/UMANG JAISWAL N/OneDrive/Desktop/SIH-21632/agrilink/.claude/gsd-core/templates/summary.md
@C:/Users/UMANG JAISWAL N/OneDrive/Desktop/SIH-21632/agrilink/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-price-discovery-i18n-shell/1-CONTEXT.md
@.planning/phases/01-price-discovery-i18n-shell/1-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/INTEGRATIONS.md

@backend/app/services/ingestion.py
@backend/app/services/fixtures.py
@backend/app/services/signal.py
@backend/app/services/geo.py
@backend/app/api/prices.py
@backend/app/core/config.py
@backend/app/schemas/price.py
@backend/app/models/price_cache.py
@.planning/phases/01-price-discovery-i18n-shell/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

New files (across all 4 plans): `backend/alembic.ini`, `backend/alembic/env.py`, `backend/alembic/script.py.mako`, `backend/alembic/versions/0001_initial_schema.py`, `backend/app/services/snapshot.py`, `backend/app/services/data/maharashtra_snapshot.csv`, `backend/pyproject.toml`, `backend/tests/conftest.py`, `backend/tests/test_signal.py`, `backend/tests/test_geo.py`, `backend/tests/test_ingestion_normalize.py`, `backend/tests/test_ingestion_fallback.py`, `backend/tests/test_api_prices.py`, `backend/README.md`, `README.md`, `frontend/vitest.config.mts`, `frontend/vitest.setup.ts`, `frontend/src/test/render.tsx`, `frontend/src/components/AppShellSkeleton.tsx`, `frontend/src/i18n/types.d.ts`, `frontend/src/i18n/messages/parity.test.ts`, `frontend/src/components/SellWaitSignalCard.test.tsx`, `frontend/src/components/PriceDashboard.test.tsx`, `frontend/src/components/LanguageSwitcher.test.tsx`.

New symbols/config: `resolve_ingestion_rows()`, `fetch_arrivals_rows()` (off-by-default seam), `merge_arrivals()`, `load_snapshot_rows()`, `Settings.ingest_trigger_secret`, `Settings.arrivals_source_url`, `NAMING_CONVENTION` on `Base`, `max_distance_km` + `limit` on `GET /api/prices/nearby`, `x_ingest_secret` header on `POST /api/ingest/run`, `ready` gate + `AppShellSkeleton`, `renderWithIntl()`, `test` / `test:watch` scripts.

New env vars: `INGEST_TRIGGER_SECRET`, `ARRIVALS_SOURCE_URL` (both added to `backend/.env.example`).

This plan (01-02) contributes: `backend/app/services/snapshot.py`, `backend/app/services/data/maharashtra_snapshot.csv`, `resolve_ingestion_rows()` / `fetch_arrivals_rows()` / `merge_arrivals()` in `ingestion.py`, `Settings.ingest_trigger_secret` / `Settings.arrivals_source_url`, `max_distance_km` + `limit` params, `x_ingest_secret` header gate, `backend/pyproject.toml`, `backend/tests/*` (conftest + 5 test modules), `pytest==9.1.1` in `requirements.txt`, `INGEST_TRIGGER_SECRET` + `ARRIVALS_SOURCE_URL` in `.env.example`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: verify pytest package legitimacy before install</name>
  <what-built>About to add one new backend dependency: `pytest==9.1.1` (canonical Python test runner, `github.com/pytest-dev/pytest`). Flagged `SUS` in the Package Legitimacy Audit only for `unknown-downloads` (PyPI exposes no weekly count to the seam). No `postinstall` hook. `respx` is NOT being added — HTTP mocking uses `monkeypatch` at the module boundary.</what-built>
  <how-to-verify>Open https://pypi.org/project/pytest/ and confirm the project is `pytest`, maintained by `pytest-dev`, latest release 9.1.x dated 2026-06, millions of downloads/week. Trivial ACK expected.</how-to-verify>
  <resume-signal>Type "approved" to proceed with `pip install pytest==9.1.1`, or describe concerns.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Extract resolve_ingestion_rows() + seed order + pytest infra (TRACER — testable source-selection path)</name>
  <files>backend/app/services/ingestion.py, backend/app/services/snapshot.py, backend/app/services/data/maharashtra_snapshot.csv, backend/requirements.txt, backend/pyproject.toml, backend/tests/conftest.py, backend/tests/test_ingestion_fallback.py, backend/tests/test_api_prices.py</files>
  <read_first>backend/app/services/ingestion.py, backend/app/services/fixtures.py, backend/app/core/config.py, backend/app/models/price_cache.py, .planning/phases/01-price-discovery-i18n-shell/1-RESEARCH.md</read_first>
  <behavior>
    - resolve_ingestion_rows() with settings.data_gov_in_api_key == "" -> returns ("snapshot", rows) when the CSV loads with rows, else ("fixture", rows); never raises; no DB access
    - resolve_ingestion_rows() when fetch_maharashtra_rows raises httpx.ConnectError -> logs a warning and returns ("snapshot", ...) or ("fixture", ...) — source is never "live"
    - resolve_ingestion_rows() when fetch_maharashtra_rows returns [] -> falls through to snapshot/fixture (source != "live")
    - resolve_ingestion_rows() when the live call returns usable normalized rows -> returns ("live", rows)
    - load_snapshot_rows() -> list[dict] shaped exactly like normalize_rows output, every row arrival_volume == None
    - test_api_prices.py::test_options_ok -> GET /api/options on the seeded SQLite client returns 200 with a non-empty list
  </behavior>
  <action>
Install pytest into the venv: `backend/venv/Scripts/python.exe -m pip install pytest==9.1.1`; append `pytest==9.1.1` to `backend/requirements.txt` after the `alembic==1.19.1` line added in 01-01.

Create `backend/pyproject.toml` with a `[tool.pytest.ini_options]` table: `pythonpath = ["."]`, `testpaths = ["tests"]`, `markers = ["pg: needs a live Docker Postgres on :5433"]`. (If a `pyproject.toml` already exists, add only that table.)

Create `backend/app/services/data/maharashtra_snapshot.csv`: a real Maharashtra data.gov.in export in the resource's exact 10-field header order `state,district,market,commodity,variety,grade,arrival_date,min_price,max_price,modal_price` with at least 30 authentic rows spanning several markets present in `geo.DISTRICT_CENTROIDS` districts (e.g. Pune, Nashik/Lasalgaon, Solapur, Nagpur, Ahmednagar) and the fixture crops (Onion, Tur, Cotton, Soybean, Tomato) so `/api/options` and the trend/nearby routes have overlap with existing selections (Pitfall 10). `arrival_date` uses `DD/MM/YYYY`. No arrivals column exists in this schema — that is expected (D-02 revised).

Create `backend/app/services/snapshot.py` with `load_snapshot_rows(path: str | None = None) -> list[dict]`: read the CSV with the stdlib `csv` module (do NOT add pandas — 1-RESEARCH.md "Don't Hand-Roll"), default `path` to the committed file resolved relative to this module, and return dicts in the same shape `normalize_rows` produces (`crop`, `variety`, `market`, `district`, `state`, `date` as a `datetime.date`, `min_price`, `max_price`, `modal_price`, `arrival_volume=None`). Reuse `ingestion._parse_date` and `ingestion._parse_float` for parsing. Skip rows missing commodity/market/date/modal_price, mirroring `normalize_rows`.

Refactor `backend/app/services/ingestion.py` per 1-RESEARCH.md "Testable ingestion refactor": add `resolve_ingestion_rows() -> tuple[str, list[dict]]` that is pure (no DB, no side effects): try the live path (guard on `settings.data_gov_in_api_key`, `normalize_rows(fetch_maharashtra_rows(key))`, raise if empty) and return `("live", rows)` — D-01: the primary resource `9ef84268-d588-465a-a308-a864a43d0070` in `fetch_maharashtra_rows` is kept unchanged as the price source; on any exception log `logger.warning("Live ingestion unavailable (%s); using snapshot/fixture data", exc)` and then try `load_snapshot_rows()` — if it yields rows return `("snapshot", rows)`, otherwise return `("fixture", generate_fixture_rows())`. Rewrite `run_ingestion(db)` to call `resolve_ingestion_rows()` then `upsert_price_rows(db, rows)` and return `{"source": source, "rows_upserted": count}`. Import `load_snapshot_rows` from `app.services.snapshot`.

Create `backend/tests/conftest.py` per 1-RESEARCH.md "pytest conftest.py" code example: a `db` fixture (`create_engine("sqlite://", connect_args={"check_same_thread": False}, poolclass=StaticPool)`, `Base.metadata.create_all(engine)`), a `seeded_db` fixture (`db.add_all(PriceCache(**row) for row in generate_fixture_rows(days=40))`), and a `client` fixture that sets `app.dependency_overrides[get_db] = lambda: seeded_db` and yields a bare `TestClient(app)` — NOT the `with` form (Pitfall 4: the context manager boots APScheduler and runs the real lifespan). Clear `app.dependency_overrides` after.

Create `backend/tests/test_ingestion_fallback.py` per the same research section: `test_falls_back_when_live_raises` (monkeypatch `ingestion.settings.data_gov_in_api_key = "x"` and `ingestion.fetch_maharashtra_rows` to raise `httpx.ConnectError`) asserting `resolve_ingestion_rows()[0]` is `"snapshot"` or `"fixture"` and rows are non-empty; `test_falls_back_when_live_empty` (monkeypatch `fetch_maharashtra_rows` to return `[]`) asserting the same; `test_snapshot_rows_have_no_volume` asserting every `load_snapshot_rows()` row has `arrival_volume is None`.

Create `backend/tests/test_api_prices.py` with `test_options_ok` only for now (more tests land in Task 3): `client.get("/api/options")` returns 200 and a non-empty JSON list.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_ingestion_fallback.py tests/test_api_prices.py -q</automated>
    <fails_when>pytest exit code != 0, OR output contains "FAILED" / "ERROR", OR the summary line does not report all collected tests passed.</fails_when>
  </verify>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -c "from app.services.ingestion import resolve_ingestion_rows; s,r=resolve_ingestion_rows(); print(s, len(r)); assert s in {'live','snapshot','fixture'} and len(r) > 0"</automated>
    <fails_when>The assert raises, OR `s` is not one of `live` / `snapshot` / `fixture`, OR `len(r)` is 0, OR the import raises (refactor broke the module).</fails_when>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/ingestion.py` contains `def resolve_ingestion_rows(` and `run_ingestion` calls it (grep `resolve_ingestion_rows` returns >=2 hits)
    - `backend/app/services/snapshot.py` contains `def load_snapshot_rows(` and imports `csv` (stdlib); `grep -c pandas backend/app/services/snapshot.py` returns `0`
    - `backend/app/services/data/maharashtra_snapshot.csv` first line is exactly `state,district,market,commodity,variety,grade,arrival_date,min_price,max_price,modal_price` and the file has >= 31 lines
    - `backend/pyproject.toml` contains `[tool.pytest.ini_options]`, `pythonpath`, `testpaths`, and a `pg:` marker
    - `backend/requirements.txt` contains a line matching `^pytest==9\.1\.1$`
    - `backend/tests/conftest.py` contains `StaticPool` and `TestClient(app)` and does NOT contain `with TestClient`
    - `cd backend && venv/Scripts/python.exe -m pytest tests/test_ingestion_fallback.py tests/test_api_prices.py -q` passes
  </acceptance_criteria>
  <done>Source selection is a pure, unit-tested function with a live -> snapshot -> fixture order; the committed CSV snapshot exists; pytest runs green against SQLite with no lifespan/scheduler boot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Arrivals seam (off by default) + normalize/geo unit tests</name>
  <files>backend/app/services/ingestion.py, backend/app/core/config.py, backend/.env.example, backend/tests/test_ingestion_normalize.py, backend/tests/test_geo.py</files>
  <read_first>backend/app/services/ingestion.py, backend/app/services/geo.py, backend/app/core/config.py, backend/.env.example, .planning/phases/01-price-discovery-i18n-shell/1-CONTEXT.md, .planning/phases/01-price-discovery-i18n-shell/1-RESEARCH.md</read_first>
  <behavior>
    - fetch_arrivals_rows() with settings.arrivals_source_url == "" -> returns [] immediately, logs one info/warning line, makes no HTTP call
    - merge_arrivals(price_rows, arrival_rows) -> price_rows with arrival_volume filled where (market, crop, date) matches; unmatched arrival rows are dropped; no new dict is added to price_rows
    - merge_arrivals with an arrival row whose key matches nothing -> that arrival is silently dropped, price_rows length unchanged
    - normalize_rows drops a raw row missing commodity, market, arrival_date, or modal_price
    - normalize_rows parses both `DD/MM/YYYY` and `YYYY-MM-DD` arrival_date formats
    - normalize_rows sets arrival_volume to None on every row
    - district_distance_km("Pune", "Pune") == 0.0 ; district_distance_km("Pune", "Nagpur") is a positive float within +/-5 km of the pinned expected value ; district_distance_km("Pune", "Atlantis") is None ; the function is symmetric
  </behavior>
  <action>
Add to `backend/app/core/config.py` `Settings`: `arrivals_source_url: str = ""` and `ingest_trigger_secret: str = ""` (the latter is consumed in Task 3; add both here to keep config changes in one place). Add matching blank lines to `backend/.env.example`: `ARRIVALS_SOURCE_URL=` and `INGEST_TRIGGER_SECRET=`, each with a one-line `#` comment ("off by default - no live arrivals source in Phase 1, see PRICE-07" and "shared secret for POST /api/ingest/run; blank disables the endpoint").

Add to `backend/app/services/ingestion.py` an off-by-default arrivals seam (Claude's discretion on internal shape, per 1-CONTEXT.md): `fetch_arrivals_rows() -> list[dict]` that returns `[]` and logs `logger.warning("No live arrivals source configured (ARRIVALS_SOURCE_URL empty) - volume factor runs on fixture/snapshot data only; see PRICE-07")` when `settings.arrivals_source_url` is falsy, and otherwise is a clearly-marked stub that a future phase fills (raise `NotImplementedError` with a message pointing at PRICE-01/PRICE-07 wiring if the URL is somehow set). Add `merge_arrivals(price_rows: list[dict], arrival_rows: list[dict]) -> list[dict]` implementing D-03: build a lookup of price rows keyed `(market, crop, date)`, for each arrival row set `arrival_volume` on the matching price row if found, drop unmatched arrivals, never append a new row. Do NOT call `fetch_arrivals_rows()` / `merge_arrivals()` from `run_ingestion` yet (the seam is dormant in Phase 1) — but add a `# Phase 1: dormant. Wire here when a non-OGD arrivals source lands (PRICE-07).` comment at the call site in `run_ingestion` showing where it goes. Update the `ingestion.py` module docstring to state that arrivals are not available from the OGD price resource and the volume factor is fixture/snapshot-powered only.

Create `backend/tests/test_ingestion_normalize.py`: cases from the "behavior" block above, driving `ingestion.normalize_rows` with hand-built raw dicts, plus one `merge_arrivals` match case and one drop-unmatched case.

Create `backend/tests/test_geo.py` (D-10: district-centroid haversine in `geo.py`, no PostGIS this phase): compute the real haversine for `("Pune","Nagpur")` once during implementation, pin it as `EXPECTED_PUNE_NAGPUR_KM` with a `pytest.approx(..., abs=5.0)` assertion; assert `("Pune","Pune") == 0.0`, unknown district -> `None`, and symmetry `district_distance_km(a,b) == district_distance_km(b,a)`. Scope per D-15: unit + one-smoke-per-route only — no e2e/Playwright, no coverage gate.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest tests/test_ingestion_normalize.py tests/test_geo.py -q</automated>
    <fails_when>pytest exit code != 0, OR output contains "FAILED" / "ERROR", OR not all collected tests pass.</fails_when>
  </verify>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -c "from app.services.ingestion import fetch_arrivals_rows, merge_arrivals; assert fetch_arrivals_rows() == []; pr=[{'market':'Pune','crop':'Onion','date':'d','arrival_volume':None}]; ar=[{'market':'Pune','crop':'Onion','date':'d','arrival_volume':42.0},{'market':'X','crop':'Y','date':'d','arrival_volume':9.0}]; out=merge_arrivals(pr, ar); assert len(out)==1 and out[0]['arrival_volume']==42.0; print('SEAM_OK')"</automated>
    <fails_when>`fetch_arrivals_rows()` does not return `[]`, OR `merge_arrivals` changes the row count, OR the matched row's `arrival_volume` is not `42.0`, OR `SEAM_OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - `backend/app/core/config.py` contains `arrivals_source_url: str = ""` and `ingest_trigger_secret: str = ""`
    - `backend/.env.example` contains `ARRIVALS_SOURCE_URL=` and `INGEST_TRIGGER_SECRET=`
    - `backend/app/services/ingestion.py` contains `def fetch_arrivals_rows(` and `def merge_arrivals(` and a `PRICE-07` reference comment; `fetch_arrivals_rows()` returns `[]` when the URL is empty
    - `run_ingestion` does NOT call `fetch_arrivals_rows` / `merge_arrivals` (grep inside the `run_ingestion` body shows only the dormant-seam comment)
    - `cd backend && venv/Scripts/python.exe -m pytest tests/test_ingestion_normalize.py tests/test_geo.py -q` passes
  </acceptance_criteria>
  <done>The arrivals seam exists, is provably off by default, and merges by (market, crop, date) without inventing rows; `normalize_rows` and `district_distance_km` are pinned by unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /nearby cap+limit, ingest shared-secret gate, signal + API tests</name>
  <files>backend/app/api/prices.py, backend/tests/test_signal.py, backend/tests/test_api_prices.py</files>
  <read_first>backend/app/api/prices.py, backend/app/services/signal.py, backend/app/services/geo.py, backend/app/schemas/price.py, backend/app/core/config.py, .planning/phases/01-price-discovery-i18n-shell/1-RESEARCH.md</read_first>
  <behavior>
    - GET /api/prices/nearby?crop=Onion&district=Pune -> 200, len(body) <= 8, every item distance_km is None or <= 200, items with a numeric distance sorted ascending, None-distance items last
    - GET /api/prices/nearby?crop=Onion&district=Pune&limit=3&max_distance_km=50 -> 200, len(body) <= 3, every numeric distance <= 50
    - POST /api/ingest/run with no X-Ingest-Secret header -> 403
    - POST /api/ingest/run with a wrong X-Ingest-Secret -> 403
    - POST /api/ingest/run with the correct X-Ingest-Secret (settings monkeypatched) -> 200 and a body with source + rows_upserted
    - compute_signal: <7 rows -> None ; 7-13 flat rows -> not None, ma_30 is None, a reason mentions "30-day comparison" ; 25 flat + 7 rising rows -> recommendation "sell_now" ; 25 flat + 7 falling rows -> recommendation "wait" ; 20 flat rows with volumes rising +30% -> a reason contains "Arrivals are up" ; 20 flat rows, no volumes -> a reason contains "Arrival-volume data isn't available"
  </behavior>
  <action>
Update `GET /api/prices/nearby` in `backend/app/api/prices.py` per 1-RESEARCH.md "nearby cap + top-8" code example (D-09): add `max_distance_km: float = Query(200, gt=0, le=2000)` and `limit: int = Query(8, ge=1, le=50)` params. Keep the existing "latest reported date for the crop" query. After building the `NearestMarketComparison` list, keep items whose `distance_km is None` OR `<= max_distance_km` (drop only known distances beyond the cap), sort with key `(item.distance_km is None, item.distance_km or 0.0)` so unknown-centroid markets stay last (current behaviour), and return `kept[:limit]`. Do not change `src/lib/api.ts` — defaults apply.

Gate `POST /api/ingest/run` per 1-RESEARCH.md "shared-secret gate" code example (D-05): add `x_ingest_secret: str | None = Header(default=None)` (import `Header` from `fastapi`, `secrets` stdlib). Compute `expected = settings.ingest_trigger_secret`; if `not expected or not x_ingest_secret or not secrets.compare_digest(x_ingest_secret, expected)` -> `raise HTTPException(status_code=403, detail="Forbidden")`. Only then call `ingestion.run_ingestion(db)`. Never log `x_ingest_secret` or `expected`; the 403 detail is the generic string "Forbidden". FastAPI maps the `x_ingest_secret` parameter to the `X-Ingest-Secret` request header.

Leave `backend/app/services/signal.py` unchanged — D-06/D-07: the weighted model and the dormant volume factor stay exactly as written. This task only adds tests that pin the current behaviour, including the null-volume "skipped" reason.

Create `backend/tests/test_signal.py` per 1-RESEARCH.md "compute_signal unit tests" code example: a `_rows(prices, volumes=None)` helper building `SimpleNamespace(date=..., modal_price=..., arrival_volume=...)` objects, and the 7 cases from the "behavior" block (D-08). Use the exact reason substrings from `signal.py`: `"30-day comparison"`, `"Arrivals are up"`, `"Arrival-volume data isn't available"`.

Extend `backend/tests/test_api_prices.py` with: `test_trend_ok` (`/api/prices/trend?crop=Onion&market=Pune&days=30` -> 200 with `points`), `test_signal_ok` (`/api/prices/signal?crop=Onion&market=Pune` -> 200, `recommendation` in `{sell_now, wait, hold}`), `test_nearby_caps_and_limits` (default call -> `len <= 8`, every `distance_km` None or `<= 200`), `test_nearby_respects_explicit_limit` (`limit=3` -> `len <= 3`), and `test_ingest_requires_secret` (monkeypatch `app.api.prices.settings.ingest_trigger_secret = "topsecret"`; no header -> 403; `headers={"X-Ingest-Secret": "wrong"}` -> 403; `headers={"X-Ingest-Secret": "topsecret"}` -> 200 with `source` in the body). The seeded SQLite `client` fixture has Onion/Pune from `generate_fixture_rows`.
  </action>
  <verify>
    <automated>cd backend && venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>pytest exit code != 0, OR output contains "FAILED" / "ERROR", OR the final summary is not "N passed" for all collected tests (excluding any `-m pg` tests, which are not run here).</fails_when>
  </verify>
  <verify>
    <automated>grep -n "max_distance_km" backend/app/api/prices.py && grep -n "secrets.compare_digest" backend/app/api/prices.py && grep -n "status_code=403" backend/app/api/prices.py</automated>
    <fails_when>Any of the three greps returns nothing (missing `max_distance_km` param, missing constant-time compare, or missing the 403 path).</fails_when>
  </verify>
  <acceptance_criteria>
    - `backend/app/api/prices.py` contains `max_distance_km: float = Query(200, gt=0, le=2000)` and `limit: int = Query(8, ge=1, le=50)`
    - `backend/app/api/prices.py` contains `secrets.compare_digest` and `raise HTTPException(status_code=403, detail="Forbidden")`
    - `grep -c "x_ingest_secret" backend/app/api/prices.py` returns `>=2`; the secret value is never passed to `logger`/`print` (grep for `logg` near `x_ingest_secret` returns nothing)
    - `backend/app/services/signal.py` is byte-identical to its state at plan start (no diff)
    - `backend/tests/test_signal.py` references `"Arrival-volume data isn't available"` and `"30-day comparison"` and `"Arrivals are up"`
    - `backend/tests/test_api_prices.py` contains `def test_ingest_requires_secret` asserting 403 (no header), 403 (wrong), 200 (correct)
    - `cd backend && venv/Scripts/python.exe -m pytest -q` passes for the whole suite
  </acceptance_criteria>
  <done>`/nearby` is bounded to <=200 km and <=8 rows with unknown-centroid markets still last; `POST /api/ingest/run` is 403 without the correct constant-time-compared header; `compute_signal` behaviour (including the dormant-volume reason) is locked by D-08 tests; full backend suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser / anonymous client -> `POST /api/ingest/run` | The one write/privileged surface in an otherwise 100%-public phase |
| browser -> `GET /api/prices/*` query params | `crop` / `market` / `district` free strings reach SQLAlchemy comparisons |
| data.gov.in -> `fetch_maharashtra_rows` (httpx) | Untrusted external JSON is normalized into `PriceCache` |
| committed CSV snapshot -> `load_snapshot_rows` | Repo-controlled file parsed into rows |
| pip -> backend/venv | New third-party package (`pytest`) enters the runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege / Denial of Service | Anonymous `POST /api/ingest/run` triggering repeated live pulls / DB writes | high | mitigate | D-05 shared-secret `X-Ingest-Secret` header; request rejected with generic `403` unless `settings.ingest_trigger_secret` is set AND matches; blank secret keeps the endpoint disabled by default |
| T-02-02 | Information Disclosure | Timing side-channel on the secret comparison | medium | mitigate | `secrets.compare_digest` (constant-time) — never `==` (ASVS V6); short-circuit order checks presence before compare so absence is not distinguishable by content |
| T-02-03 | Information Disclosure | Secret leaking into logs or the error body | medium | mitigate | Secret travels in a request header (not query string / not URL); `403` detail is the fixed string "Forbidden"; no `logger`/`print` call receives `x_ingest_secret` or `settings.ingest_trigger_secret`; verify by grep |
| T-02-04 | Tampering / Information Disclosure | SQL injection via `crop` / `market` / `district` | medium | mitigate | Values are used only as bound parameters in SQLAlchemy ORM comparisons (`PriceCache.crop == crop`); no f-string SQL anywhere; Pydantic `response_model` shapes output |
| T-02-05 | Denial of Service | Unbounded `days` / result size | low | mitigate | `days` is `Query(30, ge=1, le=90)`; `/nearby` now `Query`-bounds `max_distance_km` (`gt=0, le=2000`) and `limit` (`ge=1, le=50`) and hard-slices `kept[:limit]` |
| T-02-06 | Tampering | Malformed external JSON / CSV corrupting `PriceCache` | low | mitigate | `normalize_rows` and `load_snapshot_rows` drop any row missing commodity/market/date/modal_price and coerce numerics via `_parse_float`; upsert keyed on the unique constraint |
| T-02-SC | Tampering | `pip install pytest==9.1.1` | high | mitigate | Package-legitimacy `checkpoint:human-verify` before install (pypi.org/project/pytest — pytest-dev); exact version pin in `requirements.txt`; audit confirms no `postinstall`; `respx` deliberately not added |

Block-on-high check: T-02-01 and T-02-SC are `high`; both are `mitigate` (D-05 gate + constant-time compare; blocking human checkpoint + pinned version). No unmitigated `high` remains — consistent with 1-RESEARCH.md Security Domain "no HIGH-severity items" once D-05 is in place.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd backend && venv/Scripts/python.exe -m pytest -q` is green (all of test_signal / test_geo / test_ingestion_normalize / test_ingestion_fallback / test_api_prices)
- [ ] `resolve_ingestion_rows()` returns `live` / `snapshot` / `fixture` and never touches the DB
- [ ] Committed `maharashtra_snapshot.csv` has the exact 10-field header and >= 30 data rows
- [ ] `fetch_arrivals_rows()` returns `[]` with `ARRIVALS_SOURCE_URL` empty; `merge_arrivals` never grows the row list
- [ ] `/api/prices/nearby` bounded: `<= limit` rows, known distances `<= max_distance_km`, None last
- [ ] `POST /api/ingest/run`: 403 (missing/wrong header), 200 (correct header), constant-time compare, no secret in logs
- [ ] `backend/app/services/signal.py` unchanged
- [ ] `backend/.env.example` gains `INGEST_TRIGGER_SECRET=` and `ARRIVALS_SOURCE_URL=`
</verification>

<success_criteria>
- All tasks completed; package checkpoint resolved
- Signal volume factor is powered by committed snapshot/fixture data with the "say so" fallback intact (D-07)
- Write endpoint is not anonymously triggerable (D-05); `/nearby` is bounded (D-09)
- Backend pure logic + one smoke test per `/api/prices/*` route are green (D-13)
- No change to `signal.py`, `geo.py` behaviour; no new runtime dependency beyond `pytest`
</success_criteria>

<output>
After completion, create `.planning/phases/01-price-discovery-i18n-shell/01-02-SUMMARY.md`
</output>
