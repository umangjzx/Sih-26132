---
phase: 01-price-discovery-i18n-shell
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/i18n/LocaleProvider.tsx
  - frontend/src/components/AppShellSkeleton.tsx
  - frontend/src/i18n/types.d.ts
  - frontend/src/i18n/messages/en.json
  - frontend/src/i18n/messages/hi.json
  - frontend/src/i18n/messages/mr.json
  - frontend/src/components/PriceDashboard.tsx
  - frontend/package.json
  - frontend/vitest.config.mts
  - frontend/vitest.setup.ts
  - frontend/src/test/render.tsx
  - frontend/src/i18n/messages/parity.test.ts
  - frontend/src/components/SellWaitSignalCard.test.tsx
  - frontend/src/components/PriceDashboard.test.tsx
  - frontend/src/components/LanguageSwitcher.test.tsx
files_deleted: []
autonomous: false
requirements: [I18N-01, I18N-02, I18N-03, I18N-04, I18N-05, A11Y-01, A11Y-02, A11Y-03, PERF-01]
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On first load with localStorage['agrilink.locale']='hi' set, the app renders a locale-neutral skeleton then Devanagari content with no visible English frame and no React hydration warning"
    - "The language switcher in the header changes the UI language and persists the choice in localStorage['agrilink.locale']; document.documentElement.lang follows the selection"
    - "hi.json and mr.json contain every key present in en.json and no keys absent from en.json (parity test is green)"
    - "PriceDashboard shows skeleton placeholders while loading (each carrying data-testid='skeleton' and role='status'), then the fetched data"
    - "The dashboard error state offers a Retry control that re-runs the same fetch, replacing the previous static-only message"
    - "No user-facing string in the changed components is hardcoded — every one goes through useTranslations"
    - "`cd frontend && npm run test` runs vitest and all suites pass"
  artifacts:
    - path: "frontend/src/i18n/LocaleProvider.tsx"
      provides: "no-flash ready gate rendering AppShellSkeleton until localStorage locale is resolved"
      contains: "ready"
      min_lines: 45
    - path: "frontend/src/components/AppShellSkeleton.tsx"
      provides: "locale-neutral skeleton shown before locale resolves (no useTranslations)"
      contains: "export function AppShellSkeleton"
      min_lines: 8
    - path: "frontend/vitest.config.mts"
      provides: "vitest config: jsdom env, tsconfigPaths + react plugins, setupFiles"
      contains: "jsdom"
    - path: "frontend/vitest.setup.ts"
      provides: "jest-dom import + recharts ResponsiveContainer stub"
      contains: "ResponsiveContainer"
    - path: "frontend/src/test/render.tsx"
      provides: "renderWithIntl() helper wrapping components in NextIntlClientProvider/LocaleProvider"
      contains: "renderWithIntl"
    - path: "frontend/src/i18n/messages/parity.test.ts"
      provides: "recursive-flatten key parity test: hi/mr cover every en key, no strays"
      contains: "flat"
      min_lines: 15
    - path: "frontend/src/components/PriceDashboard.tsx"
      provides: "retry affordance + data-testid/role on Skeleton"
      contains: "data-testid=\"skeleton\""
    - path: "frontend/package.json"
      provides: "test + test:watch scripts and vitest devDependencies"
      contains: "\"test\": \"vitest run\""
  key_links:
    - from: "frontend/src/i18n/LocaleProvider.tsx"
      to: "frontend/src/components/AppShellSkeleton.tsx"
      via: "renders {ready ? children : <AppShellSkeleton />} inside NextIntlClientProvider"
      pattern: "AppShellSkeleton"
    - from: "frontend/src/components/LanguageSwitcher.tsx"
      to: "frontend/src/i18n/LocaleProvider.tsx"
      via: "useAppLocale().setLocale writes localStorage['agrilink.locale'] and document.documentElement.lang"
      pattern: "setLocale"
    - from: "frontend/src/components/PriceDashboard.test.tsx"
      to: "frontend/src/lib/api.ts"
      via: "vi.mock('@/lib/api') so the render test drives skeleton -> data without network"
      pattern: "vi.mock\\(\"@/lib/api\"\\)"
    - from: "frontend/src/i18n/messages/parity.test.ts"
      to: "frontend/src/i18n/messages/en.json"
      via: "flattens en.json keys as the source of truth and diffs hi.json / mr.json against it"
      pattern: "en.json"
---

<objective>
Harden the i18n shell and the dashboard UX to demo quality and give the frontend its first test suite (D-14, D-16, D-17, D-18, D-19, D-20, D-21; I18N-01..05, A11Y-01..03, PERF-01).

Purpose: The client-only locale (Cordova constraint, D-16) currently flashes English before `localStorage` is read; there is no CI guard that hi/mr stay key-complete; the error state is a dead end with no retry; skeletons are invisible to tests and screen readers.
Output: a no-flash `ready` gate + `AppShellSkeleton`, a `types.d.ts` `AppConfig` augmentation, a light Hindi/Marathi terminology pass, a retry affordance + `data-testid`/`role` on the dashboard skeleton, and `frontend` vitest infra + 4 test modules (parity + 3 component).
</objective>

<execution_context>
@C:/Users/UMANG JAISWAL N/OneDrive/Desktop/SIH-21632/agrilink/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/UMANG JAISWAL N/OneDrive/Desktop/SIH-21632/agrilink/.claude/gsd-core/templates/summary.md
@C:/Users/UMANG JAISWAL N/OneDrive/Desktop/SIH-21632/agrilink/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-price-discovery-i18n-shell/1-CONTEXT.md
@.planning/phases/01-price-discovery-i18n-shell/1-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md

@frontend/src/i18n/LocaleProvider.tsx
@frontend/src/i18n/config.ts
@frontend/src/i18n/messages/en.json
@frontend/src/i18n/messages/hi.json
@frontend/src/i18n/messages/mr.json
@frontend/src/components/PriceDashboard.tsx
@frontend/src/components/SellWaitSignalCard.tsx
@frontend/src/components/LanguageSwitcher.tsx
@frontend/src/components/PriceTrendChart.tsx
@frontend/src/app/layout.tsx
@frontend/src/lib/api.ts
@frontend/tsconfig.json
@frontend/package.json
</context>

## Artifacts this phase produces

New files (across all 4 plans): `backend/alembic.ini`, `backend/alembic/env.py`, `backend/alembic/script.py.mako`, `backend/alembic/versions/0001_initial_schema.py`, `backend/app/services/snapshot.py`, `backend/app/services/data/maharashtra_snapshot.csv`, `backend/pyproject.toml`, `backend/tests/conftest.py`, `backend/tests/test_signal.py`, `backend/tests/test_geo.py`, `backend/tests/test_ingestion_normalize.py`, `backend/tests/test_ingestion_fallback.py`, `backend/tests/test_api_prices.py`, `backend/README.md`, `README.md`, `frontend/vitest.config.mts`, `frontend/vitest.setup.ts`, `frontend/src/test/render.tsx`, `frontend/src/components/AppShellSkeleton.tsx`, `frontend/src/i18n/types.d.ts`, `frontend/src/i18n/messages/parity.test.ts`, `frontend/src/components/SellWaitSignalCard.test.tsx`, `frontend/src/components/PriceDashboard.test.tsx`, `frontend/src/components/LanguageSwitcher.test.tsx`.

New symbols/config: `resolve_ingestion_rows()`, `fetch_arrivals_rows()` (off-by-default seam), `merge_arrivals()`, `load_snapshot_rows()`, `Settings.ingest_trigger_secret`, `Settings.arrivals_source_url`, `NAMING_CONVENTION` on `Base`, `max_distance_km` + `limit` on `GET /api/prices/nearby`, `x_ingest_secret` header on `POST /api/ingest/run`, `ready` gate + `AppShellSkeleton` in `LocaleProvider`, `renderWithIntl()`, `test` / `test:watch` scripts.

New env vars: `INGEST_TRIGGER_SECRET`, `ARRIVALS_SOURCE_URL` (both added to `backend/.env.example`).

This plan (01-03) contributes: `ready` gate in `LocaleProvider.tsx`, `frontend/src/components/AppShellSkeleton.tsx`, `frontend/src/i18n/types.d.ts`, terminology-reviewed `hi.json` / `mr.json`, retry affordance + `data-testid="skeleton"` / `role="status"` in `PriceDashboard.tsx`, `frontend/vitest.config.mts`, `frontend/vitest.setup.ts`, `frontend/src/test/render.tsx`, `frontend/src/i18n/messages/parity.test.ts`, `SellWaitSignalCard.test.tsx`, `PriceDashboard.test.tsx`, `LanguageSwitcher.test.tsx`, `test` / `test:watch` scripts + vitest devDependencies in `package.json`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: verify vitest / testing-library batch legitimacy before install</name>
  <what-built>About to add frontend devDependencies (test tooling only, never shipped to users): `vitest@4.1.11`, `@vitejs/plugin-react@6.1.1`, `jsdom@30.0.1`, `@testing-library/react@16.3.3`, `@testing-library/dom@10.4.x`, `@testing-library/jest-dom@7.0.1`, `@testing-library/user-event@14.6.6`, `vite-tsconfig-paths@6.1.1`. The Package Legitimacy Audit flagged several `SUS` for `too-new` only — all are first-party projects with 30M-100M npm downloads/week and no `postinstall` hooks.</what-built>
  <how-to-verify>Spot-check two: https://www.npmjs.com/package/vitest (repo `vitest-dev/vitest`, ~99M downloads/wk) and https://www.npmjs.com/package/@testing-library/react (repo `testing-library/react-testing-library`, v16 = React 19 support). Trivial ACK expected.</how-to-verify>
  <resume-signal>Type "approved" to proceed with `npm i -D` of the batch, or describe concerns.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 1: No-flash ready gate + vitest infra + key-parity test (TRACER — locale path proven end-to-end and test-covered)</name>
  <files>frontend/src/i18n/LocaleProvider.tsx, frontend/src/components/AppShellSkeleton.tsx, frontend/package.json, frontend/vitest.config.mts, frontend/vitest.setup.ts, frontend/src/test/render.tsx, frontend/src/i18n/messages/parity.test.ts</files>
  <read_first>frontend/src/i18n/LocaleProvider.tsx, frontend/src/i18n/config.ts, frontend/src/app/layout.tsx, frontend/src/i18n/messages/en.json, frontend/tsconfig.json, frontend/package.json, .planning/phases/01-price-discovery-i18n-shell/1-RESEARCH.md</read_first>
  <precondition>`frontend/node_modules` is populated (`cd frontend && npm install` has been run at least once — STACK.md notes deps are installed).</precondition>
  <action>
Install the dev tooling: `cd frontend && npm i -D vitest@4.1.11 @vitejs/plugin-react@6.1.1 jsdom@30.0.1 @testing-library/react@16.3.3 @testing-library/dom@10.4.1 @testing-library/jest-dom@7.0.1 @testing-library/user-event@14.6.6 vite-tsconfig-paths@6.1.1`. Add to `frontend/package.json` `"scripts"`: `"test": "vitest run"` and `"test:watch": "vitest"`. Commit the updated `package-lock.json`.

Add the no-flash `ready` gate to `frontend/src/i18n/LocaleProvider.tsx` per 1-RESEARCH.md "Pattern 4": keep `const [locale, setLocaleState] = useState<Locale>(defaultLocale)`, add `const [ready, setReady] = useState(false)`. In the existing `useEffect`, read `localStorage[STORAGE_KEY]`, compute `next` = stored valid locale or `defaultLocale`, call `setLocaleState(next)`, set `document.documentElement.lang = next`, then `setReady(true)`. Render `{ready ? children : <AppShellSkeleton />}` INSIDE `NextIntlClientProvider` (so the provider is always mounted). Server render and first client render both compute `ready === false` and render the identical `AppShellSkeleton` -> no hydration mismatch (Pitfall 7). Do NOT add `src/i18n/request.ts`, the next-intl plugin, or middleware (Pitfall 6 / Anti-Pattern — breaks the Phase 4 static export).

Create `frontend/src/components/AppShellSkeleton.tsx`: a `"use client"`-free (it is pure markup) small component exporting `function AppShellSkeleton()` that renders a locale-neutral placeholder — a header bar and 2-3 grey rounded blocks using the existing Tailwind tokens (`bg-stone-200`, `animate-pulse`). It MUST NOT call `useTranslations` or contain any user-facing words (Pitfall 7). Give its root `role="status"` and `aria-hidden="true"` on the decorative blocks.

Create `frontend/vitest.config.mts` per 1-RESEARCH.md "Pattern 5": `defineConfig` from `vitest/config`, `plugins: [tsconfigPaths(), react()]`, `test: { environment: "jsdom", globals: true, setupFiles: ["./vitest.setup.ts"], css: false }`.

Create `frontend/vitest.setup.ts` per the same pattern: `import "@testing-library/jest-dom/vitest"`; `vi.mock("recharts", ...)` that spreads the actual module and replaces `ResponsiveContainer` with a passthrough `({ children }) => children` (Pitfall 8 — jsdom reports 0x0 so charts never draw).

Create `frontend/src/test/render.tsx`: export `renderWithIntl(ui)` that wraps `ui` in `NextIntlClientProvider` with `locale="en"`, `messages` = the imported `en.json`, `timeZone="Asia/Kolkata"` and calls Testing Library `render`. Re-export `screen` for convenience.

Create `frontend/src/i18n/messages/parity.test.ts` per 1-RESEARCH.md "i18n key-parity vitest test" (D-15: scope is focused unit/component tests only — no e2e/Playwright, no coverage gate): a recursive `flat(obj, prefix)` (~10 lines, no lodash — 1-RESEARCH.md "OK to hand-roll"), `describe.each([["hi", hi], ["mr", mr]])` with `it("contains every key present in en.json")` asserting `[...enKeys].filter(k => !localeKeys.has(k))` is `[]`, and `it("has no stray keys absent from en.json")` asserting the reverse is `[]`.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- parity</automated>
    <fails_when>vitest exits non-zero, OR output contains "FAIL", OR the parity spec reports any missing/stray key (a non-empty array in the assertion diff).</fails_when>
  </verify>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json && grep -q "AppShellSkeleton" src/i18n/LocaleProvider.tsx && grep -q "\"test\": \"vitest run\"" package.json && echo "WIRED_OK"</automated>
    <fails_when>`tsc --noEmit` reports an error, OR `LocaleProvider.tsx` does not reference `AppShellSkeleton`, OR the `test` script is missing, OR `WIRED_OK` is not printed.</fails_when>
  </verify>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
    <fails_when>`next build` exits non-zero, OR the output contains "Failed to compile" / "Error:", OR a hydration / next-intl locale error is printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - `frontend/src/i18n/LocaleProvider.tsx` contains `const [ready, setReady] = useState(false)` and `{ready ? children : <AppShellSkeleton />}`
    - `frontend/src/i18n/LocaleProvider.tsx` does NOT import `next-intl/middleware` / `./request` (no routing setup); `grep -c "request" src/i18n/LocaleProvider.tsx` returns `0`
    - `frontend/src/components/AppShellSkeleton.tsx` exports `AppShellSkeleton` and contains no `useTranslations` call
    - `frontend/vitest.config.mts` contains `environment: "jsdom"` and `setupFiles`
    - `frontend/vitest.setup.ts` contains `ResponsiveContainer` and `@testing-library/jest-dom/vitest`
    - `frontend/package.json` scripts contain `"test": "vitest run"` and `"test:watch": "vitest"`
    - `cd frontend && npm run test -- parity` passes
    - `cd frontend && npm run build` succeeds
  </acceptance_criteria>
  <done>The locale resolves through a hydration-safe skeleton gate (no English flash), vitest runs under jsdom with recharts stubbed, and the hi/mr key-parity test is green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Retry affordance + skeleton a11y/testability + their component tests</name>
  <files>frontend/src/components/PriceDashboard.tsx, frontend/src/components/PriceDashboard.test.tsx, frontend/src/components/SellWaitSignalCard.test.tsx</files>
  <read_first>frontend/src/components/PriceDashboard.tsx, frontend/src/components/SellWaitSignalCard.tsx, frontend/src/components/PriceTrendChart.tsx, frontend/src/lib/api.ts, frontend/src/i18n/messages/en.json, frontend/src/test/render.tsx, .planning/phases/01-price-discovery-i18n-shell/1-RESEARCH.md</read_first>
  <action>
In `frontend/src/components/PriceDashboard.tsx`:
- Extract the "on crop/market/days change" fetch body from the second `useEffect` into a `useCallback` named `load` (deps: `crop`, `market`, `days`, `options`); the effect just calls `load()`. This makes retry re-run the exact same fetch (D-21). In `load`, if `options.length === 0`, first `await fetchOptions()` and set crop/market/options from the result before the trend/signal/nearby calls — so a Retry after a failed initial options call also recovers (plan-checker nit 3). The first `useEffect` (mount) still calls `load()` once.
- Replace the static error `<p>` with an error panel that keeps the message (`tc("error")`) AND adds a `<button type="button" onClick={() => load()}>` labelled `tc("retry")` (the key already exists in `en.json` `common.retry`). Keep the earthy styling (red border/bg tokens already used).
- Update the inner `Skeleton` component: add `data-testid="skeleton"`, `role="status"`, and `aria-label={tc("loading")}` (key exists: `common.loading`). Do not change its visual classes.
- Do NOT introduce any hardcoded user-facing string — the retry label, error text, and skeleton aria-label all come from `useTranslations("common")` (I18N-01).
- Leave the palette tokens, `min-h`/44px, and `focus-visible` behaviour untouched (D-20) — the retry button inherits the global 44px min-height and focus ring from `globals.css`.

Create `frontend/src/components/PriceDashboard.test.tsx` per 1-RESEARCH.md "PriceDashboard skeleton->data test": `vi.mock("@/lib/api", ...)` with `fetchOptions/fetchTrend/fetchSignal/fetchNearby` as `vi.fn()`; in `beforeEach` set resolved values (one Onion/Pune option, a one-point trend, a `hold` signal with one reason, empty nearby); render via `renderWithIntl(<PriceDashboard />)`; assert `screen.getAllByTestId("skeleton").length > 0` synchronously, then `expect(await screen.findByText(/Onion/)).toBeInTheDocument()`. Add a second test: make `fetchTrend` reject once, assert the error text renders and a `screen.getByRole("button", { name: /retry/i })` is present, then `fetchTrend` resolves and clicking retry (via `@testing-library/user-event`) renders data.

Create `frontend/src/components/SellWaitSignalCard.test.tsx`: for each of `sell_now`, `wait`, `hold`, `renderWithIntl(<SellWaitSignalCard signal={{ recommendation, reasons: ["Reason A", "Reason B"], current_price: 1500, ma_7: 1490, ma_30: null, volume_trend_pct: null, days_of_data: 10 }} />)` and assert the localized label text (`Sell Now` / `Wait` / `Hold / Watch` from `en.json` `signal`) and both reason strings appear as list items.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- PriceDashboard SellWaitSignalCard</automated>
    <fails_when>vitest exits non-zero, OR output contains "FAIL", OR an `act(...)` warning is treated as an error, OR fewer than the expected test count pass.</fails_when>
  </verify>
  <verify>
    <automated>cd frontend && grep -q 'data-testid="skeleton"' src/components/PriceDashboard.tsx && grep -q 'role="status"' src/components/PriceDashboard.tsx && grep -Eq 'onClick=\{\(\) => load\(\)\}|onClick=\{load\}' src/components/PriceDashboard.tsx && echo "DASH_OK"</automated>
    <fails_when>`data-testid="skeleton"` absent, OR `role="status"` absent, OR no retry `onClick` calling `load`, OR `DASH_OK` not printed.</fails_when>
  </verify>
  <verify>
    <automated>cd frontend && grep -nE '>[A-Za-z][A-Za-z ]{3,}<' src/components/PriceDashboard.tsx | grep -vE 'useTranslations|t\(|tc\(|\{t|\{tc' | grep -vE '</?(div|section|button|label|select|option|span|p|h1|h2|dl|dt|dd|table|thead|tbody|tr|th|td)>' ; echo "literal_scan_exit=$?"</automated>
    <fails_when>`literal_scan_exit=0` with printed lines that are real user-facing literals (not JSX tags, not translation calls) — indicates a hardcoded string was introduced.</fails_when>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/PriceDashboard.tsx` contains `data-testid="skeleton"`, `role="status"`, and `aria-label={tc("loading")}` on the `Skeleton`
    - `frontend/src/components/PriceDashboard.tsx` has a `useCallback`-wrapped `load` and a Retry `<button>` whose `onClick` calls `load()`; the label is `tc("retry")`
    - The changed component introduces no hardcoded user-facing string (literal scan yields only JSX tags / translation calls)
    - `frontend/src/components/PriceDashboard.test.tsx` mocks `@/lib/api` and asserts skeleton-then-data plus retry recovery
    - `frontend/src/components/SellWaitSignalCard.test.tsx` covers all three recommendations and renders their reasons
    - `cd frontend && npm run test -- PriceDashboard SellWaitSignalCard` passes
  </acceptance_criteria>
  <done>The dashboard error state can recover via a translated Retry button, skeletons are visible to tests and screen readers, and both component behaviours are pinned by vitest.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: AppConfig type augmentation + Hindi/Marathi terminology pass + LanguageSwitcher test</name>
  <files>frontend/src/i18n/types.d.ts, frontend/src/i18n/messages/hi.json, frontend/src/i18n/messages/mr.json, frontend/src/i18n/messages/en.json, frontend/src/components/LanguageSwitcher.test.tsx</files>
  <read_first>frontend/src/i18n/messages/en.json, frontend/src/i18n/messages/hi.json, frontend/src/i18n/messages/mr.json, frontend/src/i18n/config.ts, frontend/src/components/LanguageSwitcher.tsx, frontend/src/i18n/LocaleProvider.tsx, .planning/phases/01-price-discovery-i18n-shell/1-RESEARCH.md</read_first>
  <action>
Create `frontend/src/i18n/types.d.ts` per 1-RESEARCH.md "types.d.ts": `import type en from "./messages/en.json"; declare module "next-intl" { interface AppConfig { Messages: typeof en } }` — DX only; `tsc` then flags missing keys used in code (D-19: en is the source of truth).

Terminology plausibility pass on `hi.json` and `mr.json` (D-18 — light review, NOT a professional translation): read every value against its `en.json` counterpart and the mandi domain. Record findings inline in the SUMMARY, not in the JSON. Accept "modal price" rendered as औसत भाव (hi) / सरासरी भाव (mr) ("average price") — 1-RESEARCH.md Open Questions #4 confirms this is an appropriate plain-language rendering for low-literacy users, NOT a bug. Only edit a value if it is plainly wrong (wrong word, wrong script, obvious typo). Keep all keys identical to `en.json` (the parity test from Task 1 will fail otherwise). Do NOT add or remove keys. English (`en.json`) stays the untouched source of truth (D-19) — only touch `en.json` if Task 2 required a genuinely new key (it did not; `common.retry` and `common.loading` already exist).

Create `frontend/src/components/LanguageSwitcher.test.tsx` per 1-RESEARCH.md "LanguageSwitcher test": `afterEach(() => localStorage.clear())`; render `<LocaleProvider><LanguageSwitcher /></LocaleProvider>`; `await userEvent.selectOptions(await screen.findByLabelText(/language/i), "hi")`; assert `localStorage.getItem("agrilink.locale") === "hi"` and `document.documentElement.lang === "hi"`. Add a second assertion that switching to "mr" then re-rendering keeps the `<select>` value at "mr" (persistence within the session).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- LanguageSwitcher parity</automated>
    <fails_when>vitest exits non-zero, OR "FAIL" appears, OR the switcher test does not observe `localStorage['agrilink.locale']` / `document.documentElement.lang` updating, OR parity regressed.</fails_when>
  </verify>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json && node -e "const en=require('./src/i18n/messages/en.json'),hi=require('./src/i18n/messages/hi.json'),mr=require('./src/i18n/messages/mr.json');const f=(o,p='')=>Object.entries(o).flatMap(([k,v])=>v&&typeof v==='object'?f(v,p+k+'.'):[p+k]);const e=f(en).sort().join(',');if(e!==f(hi).sort().join(',')||e!==f(mr).sort().join(','))throw new Error('key set drift');console.log('KEYS_MATCH')"</automated>
    <fails_when>`tsc --noEmit` errors, OR the node script throws `key set drift` (hi/mr key sets no longer equal en), OR `KEYS_MATCH` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - `frontend/src/i18n/types.d.ts` contains `interface AppConfig` and `Messages: typeof en`
    - `hi.json` and `mr.json` have the exact same flattened key set as `en.json` (node script prints `KEYS_MATCH`)
    - `frontend/src/i18n/messages/en.json` is unchanged from plan start (no diff)
    - `frontend/src/components/LanguageSwitcher.test.tsx` asserts both `localStorage['agrilink.locale']` and `document.documentElement.lang` update on select
    - `cd frontend && npm run test -- LanguageSwitcher parity` passes and `npx tsc --noEmit` is clean
  </acceptance_criteria>
  <done>`Messages` typing is wired for key safety, hi/mr terms have had a documented plausibility pass with no key drift, and the switcher's change+persist behaviour is pinned (I18N-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage -> `LocaleProvider` | `agrilink.locale` is attacker-influenceable client state read on every load |
| backend JSON (`reasons[]`, market names) -> React render | Signal reason strings and market/district names are rendered as text |
| npm registry -> `frontend/node_modules` (dev only) | New devDependencies (test tooling) enter the build machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `localStorage['agrilink.locale']` set to an unknown value | low | mitigate | `LocaleProvider` validates via `isLocale(stored)` before use and falls back to `defaultLocale`; the `ready` gate never trusts raw storage |
| T-03-02 | Cross-Site Scripting | Rendering backend `reasons[]` / market names in `SellWaitSignalCard` / tables | low | mitigate | React escapes text children by default; no `dangerouslySetInnerHTML` anywhere in the changed components; strings are placed as `{reason}` text nodes only |
| T-03-03 | Information Disclosure | Secrets in client bundle | low | accept | Frontend has no secrets; `NEXT_PUBLIC_API_URL` is a public base URL by design; nothing added this plan changes that |
| T-03-04 | Denial of Service / UX | `AppShellSkeleton` differing between server and first client render -> hydration thrash | low | mitigate | Skeleton is fixed locale-neutral markup with no `useTranslations`; both passes compute `ready === false` and render the identical DOM (Pitfall 7); verified by `npm run build` + the render test |
| T-03-SC | Tampering | `npm i -D` of the vitest / testing-library batch | high | mitigate | Package-legitimacy `checkpoint:human-verify` before install; all packages first-party with 30M-100M weekly downloads and `postinstall: null`; exact versions pinned; `package-lock.json` committed; dev-only (never in the user bundle) |

Block-on-high check: T-03-SC is the only `high` and its disposition is `mitigate` via the blocking human checkpoint + pinned lockfile. No unmitigated `high` remains.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run test` (full vitest run) is green — parity + PriceDashboard + SellWaitSignalCard + LanguageSwitcher
- [ ] `cd frontend && npm run build` succeeds with no hydration / next-intl locale error
- [ ] `cd frontend && npx tsc --noEmit` is clean
- [ ] `LocaleProvider.tsx` has the `ready` gate and no `request.ts` / middleware / next-intl plugin
- [ ] `AppShellSkeleton.tsx` has no `useTranslations` and no user-facing words
- [ ] `PriceDashboard.tsx` skeleton carries `data-testid="skeleton"` + `role="status"`; error state has a translated Retry button calling `load()`
- [ ] hi.json / mr.json flattened key sets equal en.json exactly; en.json unchanged
- [ ] No hardcoded user-facing string in the changed components (literal scan clean)
</verification>

<success_criteria>
- All tasks completed; package checkpoint resolved
- No visible English flash on load; hydration-safe (D-16, I18N-05, PERF-01)
- hi/mr key parity enforced by a test that runs in `npm test` (D-17, I18N-03)
- Dashboard error state recovers via a translated Retry control (D-21)
- Skeletons visible to tests + screen readers; palette/fonts/44px/focus-visible unchanged (D-20, A11Y-01..03, A11Y-02)
- Frontend test suite (vitest + RTL) green (D-14)
</success_criteria>

<output>
After completion, create `.planning/phases/01-price-discovery-i18n-shell/01-03-SUMMARY.md`
</output>
